# handover — やること台帳（main）

> **次に何をやろうか悩んだら、まずここを見る。**
> 作る予定のもの・あとで拾う思いつき・その場から漏れた取りこぼしを書き溜める場所。
> 思いついたら「思いつき・取りこぼし」へ追記して育てる。
>
> **ここは「やること」だけを置く。** 完了したら打ち消し線で残さず**消す**（完了の正本は **git log**・現況は `STATUS.md`）。
> それ以外の置き場（知見・ADR・一次情報など）の割り振りは **CLAUDE.md「管理ドキュメントの体系」が正本**——
> ここへ再掲すると片方だけ古くなる（2026-07-25 に STATUS 側で実際に起きた）。
> 打ち消し線を溜めると「やったことリスト」に化けて台帳の役目を失う（運用: memory `docs-status-vs-handover-split`）。

## ★最優先方針（2026-07-20 転換 — デフォルトUI明快化＋幅広いサイト対応）

> **背景**: 第三者にアプリを触らせた結果、「豪奢なスキンより先に直すべき本質課題」が2つ判明——①**デフォルトUI（和モダンD）が一目で分からない**、②**なろう限定では求心力が足りない**。
> **豪奢スキン（M/P/J・星図リッチ化＝ui/refine 系）は否定しない＝温存**。ただし優先度は下記 A/B の下に置く。既存のスキン/リッチ化 backlog（★UX監査残・スキン検分要裁定群・高負荷モード横展開など）は消さずそのまま保持。
> 大原則＝memory `feedback-default-ui-legible-first`（デフォルトUIは「一目でわかる」最優先・豪奢さは opt-in の付加価値）。

### 最優先A：デフォルトUI を「一目でわかる・迷わせない」設計へ
> **2026-07-23 前進: 新デフォルトUIスキン「明快K」を実装（feat/ui-playground・ユーザー目視待ち）**＝
> 検索の明快化（さがすタブ＋検索第一強調＋公式逃げ道）・画面の自己説明性（恒常ボトムナビ＋全画面明示タイトル・
> 目次の現在地チップ/ここから再開）・本棚3列密度増は **K が回答**（詳細＝STATUS §0・plan `default-ui-clarity-K-2026-07-23.md`）。
> 以下は**残り**のみ。
- **[K残] ユーザー実機目視**（本棚/さがす/設定/目次/読書/装いの間）→OK後コミット。**フィードバック第1便
  （2026-07-23 夕）は消化済み**＝書影輪郭線・タイトル配色接地・冊数バランス（真因と機序＝plan 進行記録・
  `docs/knowledge/compose-root-surface-content-color.md`）。書影輪郭線は 2026-07-24 裁定「線→影」で撤去済み
  （＝旧・未目視項目は消滅。影の実機目視は下の UIラウンド残タスクへ統合）。
- **[K残・目視OK 2026-07-26] 選択モードの状態表示・Web複数選択削除**: ユーザー実機目視で両方 OK 裁定＝縦画面分は完了（横画面の破線欠落は別項で対処中）。
- **[K形伝播・2026-07-23 ユーザー裁定「Kの形を他モックへ伝播」→モック16枚 正本昇格済み・Compose実装フェーズ進行中]**:
  D/M/P/J×〈本棚/さがす/目次/設定〉の新正本＝`skins/{bookshelf,discovery,toc,settings}-{D,M,P,J}.html`
  （K構造装置×各スキン意匠。D は fusion 発見帯を「さがす」タブへ分離＝ユーザー合格済み。確定事項と裁定の
  一次情報＝`.claude/plans/k-shape-propagation-2026-07-23.md`）。旧 D 正本（直下 toc/settings-D・discovery/ 系・
  fusion-D）は読書系を除き本4画面については skins/ 版が新正本。**残（2026-07-29 コード実測で棚卸し＝旧①「Compose実装進行中」は陳腐化・実装は完了済み**〔ボトムナビ/明示タイトル/可視⋮/さがす分離/目次K形/設定タブ＝D/M/P/J 全部済を grep 実測〕**）**:
  ①' **本棚の発見・装い導線の撤去のみ未実装**（4スキン共通。正本モックは発見導線を「さがす」へ分離済みだが実装は D=FindGuideBand＋トップバー見つける/Checkroom・M=ヘッダSearch＋SkyHorizon 導線・P=ShopBand＋装いボタン・J=FindGuideBandJ＋g-top が残存）——**撤去実装済み（2026-07-29 裁定・ADR 0021 決定7 改訂・装い入口は設定「きせかえ」に既存＝撤去のみで完結・M銘クラスタ装い/Jデッキ面クロームはモック温存どおり残置）→残は実機目視のみ**（★残1 の D 発見帯 collapse 節は帯撤去で obsolete＝目視確認後に節ごと削除。M「迎える」/J⋮の FAB 置換モック裁定はスキン後回し枠）
  ②本棚チップ不統一＝**実装側は全スキン状態フィルタで統一済み**・M/J モックも同日描き直し済み（正本直差分・実装どおり「すべて/よみかけ/未読/読了」）＝**完了** ③wardrobe-D tagline「余白の装い」逆同期（未消化・下記と同便で）。
- **[K次ラウンド・2026-07-23 ユーザー裁定] 現在の実機画面を原点として全モックへ適用（逆同期）**: K実装後の
  実スクリーンショットを正本の出発点に、`skins/*-K.html` 4枚を実装準拠へ描き直す（⋮＝キャプション行・書影輪郭線・
  ヘッダの冊数ベースライン・Surface配色接地後の実色）。wardrobe-D.html の和モダン tagline（「余白の装い」）も同便で。
  **あわせて恒久ルール強化＝①モックのプレースホルダは実データの色域を模す**（栞紙=地色同値を濃色板でごまかすと
  一体化バグを素通しする・実証済み）**②モック目視→実機目視の二段検分を必須**（モックは構図の裁定・実機は実データ
  衝突の検出＝役割が別）。
- **[K残] 装いの間のKミニチュア**: 現状トークンD委譲の自動描画で機能は成立。K らしさ（ナビ付きミニチュア）を出すかは磨き込み判断。
- **[実機スタック・対処済み（2026-07-25 計測→修正）] 残＝ユーザー体感確認と回帰固定**:
  framestats 実測で主因確定＝タブ settle 毎の隣ページ破棄→再コンポーズ（UIスレッド anim 段支配・cold P99 450ms。
  仮説①②が実証・③書影shadow ④ネストスクロールは棄却＝本棚スクロールは全構成 jank 1.6〜3.3% と健全・K/グリッド固有でもない）。
  対処＝`TabPagerHost` の `beyondViewportPageCount = 1`（隣ページ常駐化）→ 再計測で尾部激減（cold P95 42→24・P99 450→73ms）。
  **2026-07-26 続報（ユーザー報告「本棚→本文にジャム」の計測ラウンド）**: push（本棚→本文/目次）のジャムは**従来からの
  初回コンポーズ**（章/目次 70〜120ms がアニメ窓同居・07-16 計測と同水準・bVPC 無関係を A/B で確認）。一方 **pop
  （目次→本棚）に新規後退を発見・是正済み**＝K タブ化で pop 毎に tabs 全再コンポーズ＋隣ページ常駐が「さがす」初回
  コンポーズをアニメ同居させ 445ms 級 → 遷移中は常駐0・settle 2フレーム後に1へ移送で 177〜205ms（`TabPagerHost`）。
  タブスワイプ・pop とも 2026-07-26 ユーザー体感 OK（束ね検証の単発 267ms は体感に出ず＝cold 交絡と判断・残③の材料として保持）。
  - **残②**: push ジャムの本丸＝目次/本文 enter への「遷移中は軽量表示」適用（P2 本棚スケルトンと同型）。
    **案A「構造プレースホルダ」で実装済み（2026-07-29 裁定・settle+0ms＝P2同値採用・骨は theme 追従トークンで全スキン共通・縦書きも同経路）→残＝実機目視＋framestats で遷移窓の浄化確認**（+2f 案はコード内に後日実測比較枠として記載）。話数依存の跳ねは
    501話本のスポット計測（2026-07-26 束ね検証・目次→本文 最大112ms）でも従来レンジ内＝超長編特異の問題ではない。
  - **残③**: 「さがす」面の初回コンポーズ自体が重い（~700ms/2フレーム）＋ pop 後の tabs 再コンポーズ ~180ms
    ＝perfetto で実名特定してからの軽量化（旧・案3と同件に統合）。
  - **残④**: macrobenchmark（`.claude/plans/macrobenchmark-kickoff-2026-07-17.md`）へタブスワイプ＋遷移シナリオを足して回帰固定。
  - 距離2ページ復帰の単発 65〜73ms（頻度低）は残③に包含。案2（isScrollInProgress 連動 defer）は補助策として温存。
- **[UIラウンド 2026-07-24] 実機目視残**: 破線間隔4/3dp（縦画面）・紙地沈め alpha0.05（2026-07-26 目視便で言及なし＝未裁定のまま残）。
  ※影ダーク強化・気分ループ・横画面5列・リスト破線/案A・ソート是正は 2026-07-26 に実機＋ユーザー目視で合格済み（git log 正本）。
  裁定の一次情報＝`.claude/plans/ui-density-swipe-round-2026-07-24.md`。
  非Kスキンの気分は CLASSIC 固定＝ページャ化・日替わりの各スキン適用は別ラウンド（J の扉 glyph が P1 前提）。
- **読書の明快さ**: 本文調整/トグルの**説明**を出す（何のトグルか分かるように）／文字サイズ・本文の開き（行間・余白）の**調節バーを触っている間は、そのシートを半透明化して本文を透けさせ、変化をライブプレビュー**させる。
- **本棚・操作**: ①表示冊数増＝**K の3列グリッドで回答済み**（D 自体の密度変更は K が既定になったため取り下げ・要望再燃時のみ）／②Web作品の複数選択削除＝**K形伝播第2波で実装済み（2026-07-23）**: 可視⋮の全スキン化が長押しの代替導線となり要裁定停止が解消（旧裁定選択肢(A)相当が K形で自然成立・確認文言の出し分け(C)も同梱。キー＝蔵書 bare id + Web のみ `"web:<ncode>"` の差分方式＝既存一覧班の蔵書選択を不変に保つ Why 込み・詳細は plan `k-shape-propagation-2026-07-23.md`）。**残＝実機目視のみ**／③削除（複数選択）モードが「いま何のモードか」分かる状態表示／④長押し時の触覚フィードバック（振動 haptic＝**後回しでOK**とユーザー明言）。

### 最優先B：幅広いサイト対応＝汎用オフラインDL基盤（検索→DL→アプリ内で読む）
- なろう限定を脱し、**汎用の取得/抽出基盤**でハーメルン/アルファポリス等も見据える（サイトごとに抽出器を分離できる設計）。オフラインDL＝手元の**蔵書コレクション**の位置づけ。
- **利用規約で禁止のサイトは除外・別対応**（一律スクレイピングしない＝ユーザー裁定）。なろうは公式API＝安全で優先。**公式サイト直行の逃げ道**も併設。
- スクレイピングは**サイトのHTML変更で壊れやすい**前提で設計（脆さは織り込み・公式直行の逃げ道がその保険）。

### 着手順序・branch（2026-07-20 裁定）
- **まず最優先Aの明快さ・バグを一掃**（数日で"他者が困らない"状態へ）。最優先Bの中核設計は並行で下ごしらえ。
- **branch**: 2026-07-23 の全ブランチ統合で ui/refine / ui/wa-modern は削除済み（いずれも main 同一＝固有コミット0で失うものなし。リッチ化の**成果物**は main に全て入っている）。リッチ化・基礎開発とも再開時は main から新ブランチを切る。当面の記録はこの台帳へ集約。

### 汎用DL基盤 実装トラック（main 統合済み 2026-07-23・一次情報＝`.claude/plans/scraping-foundation-design-2026-07-20.md`）
> 設計判断 D1〜D6・調査ダイジェスト（受け皿アーキ／サイト規約）・カクヨム実構造・フェーズ順は plan が正本。
> **P2（scrape/ アダプタ抽象＋カクヨム抽出器＋fixtureゴールデン）は着地・JVM 常時緑**（完了の正本＝git log）。

- **[裁定確定 2026-07-23] 規約グレー勢のまとめ裁定＝ハーメルン保留・他4サイト NG（正本＝ADR 0024 追記）**:
  アルファポリス/Pixiv/野いちご/ベリーズカフェは NG＝blockedHosts（公式送り）へ。**ただしグレー領域の保守裁定であり
  規約明文違反の認定ではない**（包括条項の保守読み・詳細と再訪の道＝ADR 0024 追記 2026-07-23）。ハーメルンのみ
  pendingHosts で保留続行。**技術検討メモは温存（ユーザー指示）**＝**注1 Pixiv: R-18 はログイン必須＝アプリ内ブラウザ認証
  （Cookie/セッション保持）が前提**・メンバーページ登録もログイン要／**注2 アルファポリス: 連続DL制限あり＝Crawl-delay 厚め＋
  制限検知バックオフ・リトライ**。注2 の土台は実装済み（ScrapeHttpClient per-host スロットル＋429/503 Full Jitter バックオフ・
  Retry-After 尊重・403/404 即中止）＝いずれのサイトも再裁定で解放されれば blockedHosts 行削除＋アダプタ追加のみで着手可。
- **P3〜P6 全フェーズ着地（2026-07-20・完了の正本＝git log・追加裁定の正本＝ADR 0024 追記）**。
- **実機検証 全5点 PASS（2026-07-20・OPPO PGEM10）**: ①v19→21 migration 通過（user_version=21・新3列・既存行保存・
  クラッシュ0）②カクヨム実URL取込（ACTION_SEND→39章同契約HTML→本棚表示・sourceUrl/sourceSite/contentSha256 焼付け）
  ③Blocked なろうURL→公式サイトへ即送客（books 増えず）④ヘルスボード実疎通 OK（593章・先頭1760字）⑤Web蔵書の
  目次/本文/実ルビ描画。**実機は v21＝旧 v20 以下の APK は入れない**（migration 逆走禁止。2026-07-23 統合で
  レーン分立は解消済み＝統合後 main の v21 は identity hash 互換）。v19 DB バックアップ＝セッション scratchpad（消滅可・空DBだったため価値は形式的）。
- **[Web取込スナックバー残留＝実機検証 PASS（2026-07-25・カクヨム8話で実写確認）]**: 取込中バナー進行（章 i/N）・
  完了文 Short 自動消滅・Indefinite 残留なし・重複作成なし（機序と対処の一次情報＝git log とプラン）。
  **4点とも実装済み（2026-07-29 裁定「全部直す」）**: ①停止の Web 実効化（Job保持→cancel・章境界中断）②ステッパーPDF限定出し分け（Web は章 i/N 一本化）③PDF/Web 状態の Hub 分離（相互上書き根治・PDF優先合成）④重複スナックバー集約「N件は取り込み済みです」。**残**: 実機目視（停止・同時実行・集約）＋**スキン残＝M `SkyProcessingBanner`/P `WritingBanner` の Web 0%凍結**（`source` フィールド配布済み＝各1行の出し分け・スキン後回し枠）＋Web一括の本間でバナーが一瞬畳まれる軽微ちらつき（割り切り済み・気になったら）。
- **[調査済み 2026-07-23・OK側サイト実地照合（Web一次ソース）→ 次アダプタ＝暁を選定]**: 閉鎖確定＝魔法のiらんど
  （2025/3/31 終了→カクヨム合併）・フォレストページ（2024/8/27 終了・データ削除）・Feard（2023/3 頃閉鎖と推定）＝候補から恒久除外。
  **暁（akatsuki-novels.com）＝生存・SSR静的・robots 実質無制限・規約に自動取得/私的複製の禁止明文なし（著作権法再掲のみ）**
  ＝ADR 0024「着手時個別照合」を満たし裁定不要で着手→**アダプタ着地・実機全5点 PASS 済み（完了の正本＝git log・STATUS）**。**野いちご/ベリーズカフェは規約全文照合の結果 Blocked 相当**
  （2026-07-23 一次確認: 両者とも第5条に「（サービス内容の）複製・公開・送信…転載・再利用しないことに同意」の包括禁止明文＝
  端末内保存が「複製」に直接該当。robots は Allow だが規約優先・保守判定。前文「個人的使用の為に提供」との緊張＝私的複製抗弁は
  法解釈依存のため、最終扱いはアルファポリスと同じ**まとめ裁定**へ合流）。シルフェニアは「多重投稿原則禁止」
  方針＝転載嫌いの色が濃く不適の可能性。Arcadia は管理者不在で衰退。エムペ！/ナノ/ALICE+/BLove は規約未確認のまま（着手時照合）。
- **[裁定済み 2026-07-23] 汎用アダプタ方針＝GO／規約グレー勢のまとめ裁定＝引き続き保留**: ユーザー裁定
  「汎用アダプタでいいよ・（まとめ裁定は）保留で」。→ OK側の小規模サイト・個人サイト向けに競合式
  「汎用抽出エンジン＋サイト設定表」型の GenericSiteAdapter を設計・実装する（設計から着手・裁定待ち勢
  〔ハーメルン/アルファポリス/Pixiv/野いちご/ベリーズカフェ〕はこの汎用機に**載せない**＝Blocked/保留ゲート不変）。
  **設計正本＝`.claude/plans/generic-adapter-design-2026-07-23.md`**（1プロファイル=1アダプタ・Kotlin定数表・
  G1=エンジン＋暁移植＋pendingHosts ゲート**＝着地済み（JVM 854件緑・完了の正本＝git log）**／
  G2 ヒューリスティック見送り（要否は別裁定）／G3=recon 済み（下記）。
  **G3 recon 結果（2026-07-23・実地照合）＝表駆動の新規候補は実質ゼロ**: ナノ/エムペ！/ALICE+ は無料HPレンタル型
  （ユーザー毎に構造が異なる＝設定表1行で表現不能。ナノ規約に自動取得制限・エムペは bot に 403 全遮断＝載せない判断・
  ALICE+ も規約に IP 侵害系制限）。Arcadia は TLS 証明書期限切れで規約・構造とも検証不能（載せる前に再確認要）。
  **→ OK側の表駆動候補は暁で打ち止め。［裁定済み 2026-07-23］G2 ヒューリスティックは今はやらない
  （ユーザー「キリがない」）**＝スクレイピング対応面の拡大はここでいったん打ち止め。将来の解放条件＝ハーメルン裁定
  or グレー勢の再裁定（ADR 0024 追記）or 新規 SSR サイトの発見（表1行＋fixture で即追加可）。
- **[参照資料] 競合のスクレイピング実装解析（2026-07-20 ユーザー作成）**: `/mnt/c/Users/qingj/Desktop/project/book-api-analysis/07-competitor-scraping-techniques.md`＝唯一の実スクレイプ競合 B（約38サイト・jsoup・3抽出戦略・per-host レート制御/WebView Cookie 間借り等の「作法」）のデコンパイル解析。**内容が濃いため直読みせず、新アダプタ設計時に委譲ダイジェストで参照**（ユーザー指示）。

## 思いつき・取りこぼし（随時追記）

- **[試験実装済み・採否は実機所感で後日裁定（2026-07-29 ユーザー）] さがすランキングの期間 横スワイプ**: 全6期間を内側 Pager 化・端でも外側タブへ非伝播（nestedScroll 封止＝副作用で端の stretch overscroll 無し）・期間タブはページ連動・**単独コミット隔離済み＝差し戻しは当該 feat コミットの revert 一発**（件名「さがすランキングを全6期間の横スワイプPager化」・import 残りは無害な未使用警告のみ）。新 APK で触った所感→採用 or revert を裁定。

- **[性能・実測済み／切り分け残（2026-07-29）] 第三者実機（Huawei P30 / Android 10 / EMUI・60Hz）で読書画面の jank を実測**: debug 版・稼働1h45m の累積で **Janky 10.11%（122/1207）・99%tile 61ms・0.4〜0.5秒級の停止4回**。crash/ANR はゼロ。内訳は **draw 8.14ms + GPU 発行 11.29ms が支配的で traversal は 0.06ms＝レイアウトは無罪**（`RubyText`/`ChapterContent` は最適化済みのため**決め打ち修正はしない**）。実測値・回収手順・EMUI 固有の罠＝`docs/knowledge/emui-p30-jank-log-collection.md`。
  - ⚠️ **当該端末は現在 release(R8) 版が入っている**（2026-07-29 に debug 署名で `install -r`・蔵書DB保持・`run-as` 不可＝prefs/DB は読めない）。debug へ戻すには debug APK を `install -r`。
  - **残①（次にやること）＝release 版の実使用 gfxinfo の回収**。方針は **「持ち主にしばらく普通に使ってもらってから回収」（2026-07-29 ユーザー裁定）**＝こちらから起動・input はしない（実使用同士でないと debug の 10.11% と比較する意味が薄れるため）。回収手順:
    ```bash
    adb-bridge                                     # 先にこれ（WSL は毎回）
    adb shell ps -A | grep novel                   # ★プロセス生存を先に確認（死んでいたら統計は消えている＝やり直し）
    adb shell dumpsys gfxinfo com.novelreader      # 非破壊で吸う。reset や force-stop はこの後
    ```
    比較対象（debug 版・稼働1h45m）: `Janky 122/1207 = 10.11%` / 99%tile 61ms / 400ms=1 450ms=1 500ms=2。
    **gfxinfo はプロセス生存中の累積**＝アプリが kill・再起動されると消えるので、使ってもらった後は早めに吸う。
    `logcat -G 16M` の拡大は**端末再起動で既定へ戻る**ため、その場合は回収前に打ち直す。
  - 残②: 画面遷移の境目で `dumpsys gfxinfo com.novelreader reset` を打ち、本棚/さがす/読書のどれが重いか切り分け（累積統計は時刻を持たず画面を特定できないため）。
  - 残③: それでも足りなければ Perfetto で関数レベルまで（EMUI での動作は未検証・ColorOS では別端末で hang 実績あり）。

> レビュー中・実装中に出た宿題や着想で、まだ下の各節に整理していないものをここへ。育ったら該当節へ移す。

- **[実装済み・実機確認待ち・「戻る」操作の階層統一]**（2026-07-19 ユーザー裁定→2026-07-23 実装・設計正本＝`.claude/plans/back-unification-design-2026-07-23.md`）: `ReadingBackStack.back()` を「実経路逆再生」から「階層 up」（章→目次・目次→null＝本棚）へ再定義し、システム Back／左上←の全経路を統一（07/15「直行なら Back 1発で本棚」の意図的撤回・不変条件テストで固定）。**実機確認済み（2026-07-25・K/D 両スキン・本文経由/直行/←/タブ回帰の全経路 PASS）**——その過程で「目次→本棚が pop 黙殺で幽閉」の実バグを発見・同日根治（K タブ化で bookshelf ルート消滅の取り残し＝`popToBookshelfTab` へ単一化・`ReadingEscapeNavigationTest` で固定）。**残**: ①直行入場 Back 2段＝**2026-07-29 裁定「階層up維持で確定」＝解消** ②発見サブツリー（結果一覧/詳細）＝**階層up一本化 実装済み（2026-07-29 裁定「わかりやすく」・←＝Back＝詳細→直近result→ホーム・検索へは「条件を変更」明示導線のみ・ADR 0026 新設〔旧D統一は未起票と判明→正式記録の上で改訂〕）→残は実機目視のみ** ③**[2026-07-29 実機PASS＝完了]** 07-25 の幽閉修正は読書側だけを張り替え、**発見側に残った消えたルート `"discovery"` への pop 3箇所を取り残していた**＝検索→結果一覧→作品詳細の ← が完全無反応（ユーザー実機報告）。`popToTab(nav, pager, KTab)` へ一般化し、pop 先をルート名リテラルで書けない形へ閉じて欠陥クラスごと封鎖（ADR 0022 追記・`DiscoveryUpNavigationTest`）。2026-07-29 実機で (a)(b)(d) PASS・(c) は構造上実機不可（Supported URL は即取込＝詳細に入る経路が無い）＝JVM `DiscoveryUpNavigationTest`/`ReadingEscapeNavigationTest` 固定で完。
- **[要裁定・縦書きモードで上部タイトルが縦書き本文と重なる]**（2026-07-19 ユーザー報告→2026-07-23 調査済み・裁定材料あり）: 実体は共有 TopAppBar の**章タイトル**（作品名でない）で**既にクローム連動**（没入で退避＝`NativeReadingScreen.kt:1684-1747`）。重なりの真因＝縦書き本文が上端クリアランスを意図的に省略（`VerticalChapterContent.kt:113-132`・横書きの `ReadingBodyTopExtra=64dp` を「横画面で列高約4割潰れ＋クローム追従リフロー」回避のため不採用と理由コメント明記）＝バー可視時に列上端がバー下へ潜る。モック正本 `reading-vertical-scroll-D.html` は「非没入時は本文がバー下から開始（通常フロー＝重ならない）・没入時はバー消去」を規定＝現状はモック非準拠の実装差。**(a) 縦書き時タイトル非表示＝実装済み（2026-07-29 裁定・章題は item[0] 章見出しが担う）→残は実機目視のみ**。
- **[要裁定・読書M下部バーの微星装飾]**（2026-07-19 読書リッチ化で見送り）: M は共有 BottomAppBar を素で再利用しており星を描く面が無い。忠実実装には M 専用バー面の新造が要る（発明回避で見送り）。要るなら意匠込みで裁定を。
- **[宿題・richness モック正本の形状統一反映]**: toc-M-rich-R1 / discovery-M-rich-R1 モックは画面別seed時代の空のまま（Compose は一枚化で統一済み）。正本昇格時に空レイヤを R1s 形へ差し替える（一次情報＝`.claude/plans/richness-expansion-round-2026-07-19.md` 差し戻し節）。
- **[高負荷モード横展開（ADR 0023・2026-07-19 ユーザー裁定）]**: 各UI/スキンの画面へ「高負荷モード（美しさ特化・負荷許容）」を順次追加していく長期方針。**星図M試作ラウンドは v7.5 まで完成・実機合格・コミット済み**（チャンク式無限生成の粒天の川・天体系・奥行き3件・検分ボタン6種・jank 2.56%）。残＝①星図M v8（月齢/惑星/流星群）・v9（変光星/ジャイロ視差）＝ロードマップと全裁定は `.claude/plans/richness-expansion-round-2026-07-19.md` ②知見の和モダンD展開→以降各画面 ③製品トグルの置き場所・既定値・reduce-motion 優先関係の確定（現状は debug トグルのみ＝ADR 0023 の宿題）。
- **[スキン・候補] 2026-07-25 モデルA/B生成実験の生存2案**: 製図室（青写真）＝`skins/candidates/bookshelf-seizushitsu.html`・カプセル売場（ガチャ）＝`bookshelf-capsule.html`（12案中この2つのみユーザー合格）。正式スキン化は別ラウンドで。起案手順の知見＝`docs/knowledge/skin-concept-first-mock-second.md`（コンセプト行で数打ち→当たりだけモック化）。
- **[スキン・着想] アニメ等のキャラクターをもとにしたスキンモック**（2026-07-17 ユーザー着想）: 次のスキン起案ラウンドでは、アニメや何かのキャラクターをモチーフにモックを作るのも面白いかも。着手時の論点＝実在IPの意匠・名称は権利面の検討が要る（特定作品の直写でなく「キャラクター的な世界観の翻案」に留めるか、の裁定から）。
- **[スキン・改善案バックログ（2026-07-17 M/P/J実装セッションで採取）]**:
  - **[要裁定・J扉/升の色相偏り]**（2026-07-18 最終検分[medium]）: fmix32後も実測 5/6 扉が緑系（104-129°に密集・紫1・琥珀0）。真因の一半は**パレット自体が緑系2/4（薬=森金・草=森緑）**＋森の世界観。選択肢=(a)森テーマとして許容 (b)モックの4世界の色相差を広げる改訂（薬を金寄りに等）(c)割当を「直近と異色相優先」の巡回に。モック正本の改訂を伴うため要ユーザー裁定。
  - **[目視確認待ち・M観測弧の存在感]**: 検分員所見＝観測野帳の「進捗=観測弧」がレール/ノードに埋もれ気味。ユーザー実機目視で弱ければ弧の視覚強化（太さ/明度）を後詰め。
  - **[精査待ち（2026-07-17 ユーザー裁定=後に精査）]** ①P章扉のpixel話数を一段強調（モック追補要） ③HI-SCORE1位金の微燐光パルス（P静止則 ADR0022§3 の例外承認要） ④J一覧の栞先端色を扉ambientに連動（④は一覧是正の設計と合流して精査）。
  - **発見Jの不足トークン棚卸し**: 扉固有森リニア #1A2A1F 等・回廊森 rgba(31,52,38,.55)・光条α群（DiscoveryPortalJ.kt 設計コメント参照）。本棚Jは Amb*Portal パレット化済み＝同じ流儀で。あわせて本棚Jの「薬と草の base が近縁」＝色相の飛びが実機でまだ弱ければ base ストップ追い込み。
  - **settings-J 不足4値のトークン化**: --sheet #141C15・--sheet-line・扉プレビュー大気3値（現状は面追従＋金縁で機能等価・大気重ねはトークン補充後の後詰め＝ReadingPortalJ §3参照）。
  - **J2敷居光×「次章」ボタンの重なり**: 章末＋クローム表示時に右端の金緑グローが次章ボタンに薄く載る（タップ透過・機能影響なし）。心地よさ or chrome中は抑制、を実機目視で判断。
  - **Jのglyph 180sp固定**: 極端に短い章題・広幅端末での見え方は実機微調整の余地。
  - **J内側半透明白の base val 新設**: 目次Jの --soft/--dim/--line は GlyphDarkPortal の RGB 借用（α差替で値は厳密一致）＝意味が読みにくい。`SoftTocPortal` 等 or base val 1本の新設で意図明快化（値不変）。本棚Jの .resume ≒InkPortal 近似の厳密化も同時に。
  - **J目次に書籍文脈が届かない**: 目次モックの象徴文字glyph（作品の象徴）は目次画面が書籍ID/題名を受けないため省略中。glyph を目次でも出すなら骨格のシグネチャ拡張が要る（発明回避で現状見送り）。
  - **J象徴文字glyphのsemantics**: 極淡96spの装飾テキストがTalkBack読み上げ対象になり得る→ノイズなら `clearAndSetSemantics {}` を検討（モック忠実性優先で未対応）。
  - **M昇華アニメ＆P1押印アニメのトリガ配線（同型課題）**: 「読了の瞬間」イベント（reachedEnd false→true 遷移）が本棚Composableに流入しないため両者とも静的表示で代替中。栞の `playSealStamp/onSealStamped` ラッチと同型の配線を骨格から通せば一度きり再生可能（P1側 KDoc に設計メモあり）。
  - **P2現像の実カード昇格**: 現状は取込中バナー内の現像ラベル。ProcessingState から仮カセットカードをラック先頭に描けば「ラックへ着床」体験になる（未生成の本を並べる placeholder 方針の設計判断が先）。
  - **カセットカードの semantics 整備**: 読了カセットの contentDescription に「読了・CLEAR」等を付す（現状カード clickable に説明なし・モックの aria-label 相当）。
  - **読書時間の計測データ新設**: P本棚LCDの TIME 表示（捏造回避で現在非表示）と将来の統計に使える。セッション累積の記録機構が要る。
  - **連続読書日数（streak）の記録新設**: P3『連続プレイの炎』のデータ源。読書日付履歴が無ければ prefs で日付集合を持つ最小実装から。
  - **J扉の incipit（冒頭一文）**: BookEntity に synopsis 相当が無く省略。抽出時に第1章冒頭を保存すれば表示可能。
  - **P読書の浮遊puck**: モックの没入時浮遊操作（目次/設定）は共有tap-to-reveal に畳んだ。実機で不便なら独立部品化。
  - **P設定のLCD値チップ・液晶スウォッチ型テーマ選択**: 標準部品を優先して未採用。P密度を上げたければ再検討。
  - **[観察・未確定] J読書の中央タップトグルが1回空振りに見えた**（2026-07-17 C3検証員所見）: adbタップ精度の可能性大（過去のスナックバー切り分けと同型）。人間の実機体感で再現したら計測タスク化（決め打ち修正しない）。あわせて「トグル時に軽い状態表示」の改善案あり。
  - **一覧是正後のデッドコード整理（/stale-check 時に一括）**: 共有 TopAppBar 内の SEIZU_M/CARTRIDGE_P トグル分岐は一覧是正で到達不能化（M/P一覧が Scaffold を使わなくなったため）。挙動影響なしのため温存中＝J是正後にまとめて削除。
  - **Pヒンジのアクセシブル代替**: ドラッグ専用のためキーボード/スイッチ操作の段送りが無い（正本モックの ArrowUp/Down 相当）。tap-to-cycle か semantics カスタムアクションの追補を検討。あわせて段の取り分（HingeDetentsDp=56/180/260）の体感は実機微調整可。
  - **だぶり修正の横展開候補**: ①「続きに戻る」チップ（NativeReadingScreen 参照ジャンプ）も同構造の半透明ピル＝暗色スキン×明色要素で透けうる（稀な状態のため未対処） ②題末区切りダッシュのトリムは J目次のみ＝データ由来なので D/M/P 目次・章扉にも潜在（要すれば共通ユーティリティ化して全面適用）。
  - **hashCode直割当の偏り横展開**: J扉パレットは fmix32 撹拌で是正済み（docs/knowledge/string-hashcode-low-bit-bias-palette-skew.md）。同型の M `idColorFor`・P `labelColorFor` も偏りが目視で気になったら同適用。
  - **M視差の信号源精密化**: 代表セル高150dp×indexの近似（境界で最大12px段差の理論値）→実機で目につけば実測高の累積へ。※2026-07-18 セルは内容追従高（heightIn min）になった＝近似誤差は微増だが同じ割り切りの範囲。
  - **[要裁定・2026-07-18 スキン検分ラウンド（UX24層チェックリスト52項目×実機29枚＋モック突合済み）]** 以下はすべて**モック正本由来＝直すなら意匠変更**のためユーザー裁定待ち（一次情報＝`.claude/plans/ui-refine-richness-round-2026-07-18.md`）:
    - ①J本棚の表示モードトグル: D/M/P は代替モード=「一望リスト」だが J だけ「1冊全画面デッキ」＝同一トグルの情報密度が逆向き・アイコンも J のみ開本（他は≡）。統一するか J の本性として許容するか。
    - ②D本棚の「N話」だけ総話数（M/P/J は現在話）。D はモック mokuroku-D 由来で正当・ただしスロット意味のスキン間割れは実在。どちらへ統一するか要裁定。
    - ③M読書クローム章題の淡さ（--dim 由来）＝「現在章の把握が弱い」検分所見 vs クローム最小化のモック意図。
    - ④M装いの間入口が✦星（他スキンはハンガー）・⑤M設定はテーマ固定1枚（supportedThemes=[DARK]）——いずれも星図の本性 vs 共通操作語彙の統一、の裁定。
    - ⑥読書クローム「表示設定だけアクセント色」は D 由来の「一画面一強調」設計＝M/J モックも同設計。ただし P だけはモックのコンソール4ボタンが全て --ink＝共通実装がP に緑アクセントを付す微差あり（P準拠に直すか共通のままか）。
    - ⑦ADR 0022 §3 は「P/J モックに keyframes ゼロ＝静止で実装開始」と記すが、reading-J モックには .edge breathe 2.6s が実在し実装済み（J章末敷居光）＝ADR 記述と実態の矛盾。ADR 追記（J の敷居光を承認例外に）か実装抑制かの裁定。
    - ⑧構造穴の残り: NativeReadingScreen/ReadingSettingsSheet のスキン差分は「加算的クローム/値選択」で exhaustive when 化の対象外＝新スキン追加時にシート色・クローム欠落が無音で起きる残存リスク（是正は SkinTokens 化など別機構・ルーター30分岐は when 化済み）。
  - **[磨き込み候補・同検分のグレー所見（裁定不要・リッチ化ラウンドの入力）]**: M発見のカード枠/ジャンルchip境界が星空地で薄い・M本棚の未読/読了chipの淡さの意図確認・J/P の非選択チップ/タブ文字が4.5:1近傍・J発見「今夜の一気読み」カードのみ青紫（パレット外の意図確認）・P情報密度/太ベゼル/没入SAVE帯の声量・P本棚LCD版の主CTA（上端の小さな赤▶）が弱い・M本棚一覧の天体ドット多色の意味整理。
  - **J時刻大気の発展**: ①時刻3相の base/floor 色相トークン化（現状は温度・明るさのみ可変＝地の色相は森トークン固定） ②長時間常駐で時間帯を跨いだときの追従（現状は起動時1回固定。produceState＋5〜10分ポーリングが拡張余地・モーション無し方針との両立要検討）。
  - **リッチ化の横展開（fresh セッションで実施＝2026-07-19 ユーザー裁定）**: 深空リッチ化は本棚Mのみ→M目次/発見・P質感・J発光層へ「R1級」展開。**引き継ぎ書＝`.claude/plans/richness-expansion-brief-2026-07-19.md`**（R1の型10技法・受け皿の現状・モック委譲仕様3体分・検分知見・進め方まで収載＝着手時はこれを全読してから）。
- **[本棚/全スキン] ⋮メニュー上端にアプリバー裏の背後文字が覗く（D由来の持病・軽微）**（2026-07-17 M実機検証で発見→切り分け済み）: 本棚⋮の DropdownMenu がアプリバー直下に置かれ、バーとポップアップ上端の隙間に背後コンテンツの文字が覗く。D/M共通で再現（M は夜天の明度差の小ささで目立つだけ＝M退行ではない）。直すなら DropdownMenu の offset/アンカー調整。根拠スクショ=検証時 scratchpad（消滅可・再現は実機⋮開くだけ）。
- **[機能②] U1 新着チェックへの Web 既読統合＝実装済み（2026-07-29）**。設計＝Web 蔵書の既読は実測で `progress` テーブル側（委譲前提の web_reading_progress は誤り）・「最終章まで読んだ本のみ」目次再フェッチの照会最小化ゲート・SiteAdapterRegistry 経由（Blocked 化で自然停止）・marks へ `web:` キー同居。**残**: ①実機は次の Web 新着発生時に自然確認 ②**新宿題＝通知後の「続き取得」導線が未整備**（同一 URL 再共有は Duplicate ガードで弾かれる＝差分更新の導線は別途設計）③本棚「続きあり」バッジへの Web 新着反映の配線（marks を読む側＝小）。
- **[目次/本文] モーション最適化 — 残りは P1＋P2体感確認のみ**（2026-07-15）: 確定差分は適用・**遷移は実機確認済みで slide push に統一**（ADR 0019・ルート遷移＋目次⇄本文を同じ向き/尺 250ms＝進む右→左/戻る左→右）／本文 stretch オーバースクロール無効化（D2・目次/本棚は据え置き）。競合解析＝`docs/reference/06-competitor-reading-motion.md`。**残**: **P1**＝章→章（話送り）は**スワイプ経由はスライド化済み**（2026-07-16 引っ張りプレビュー実装＝ドラッグ追従＋確定スライド）・**ボタン（前章/次章）経由のみ瞬間のまま据え置き**＝要望が出たらスライド化／**P2＝実装済み・体感確認待ち**（2026-07-16 スケルトン差替えで実装＝遷移中は本棚グリッドを BookshelfSkeleton へ差替え。framestats で pop アニメ中 6〜15ms に浄化・最悪 104.8→71ms・差戻しヒッチ無しを実測。**残＝①ユーザー体感確認〔本棚へ戻る際スケルトンが一瞬見える見え方の可否含む〕②残るなら次の的は目次画面の初回コンポーズ 93/81ms〔P2対象外の副次〕を別タスク化**。経緯・全数値＝`.claude/plans/reading-transition-jank-measurement-2026-07-16.md` 末尾「修正実装と再計測」節）。
- **[本棚/通知] スナックバー「閉じる」疑い＝白・残るは複数重複時の連続再表示UXの要否判断**（2026-07-16 実機切り分け済み）: 配線健全・bounds 中心の精密タップで確実に効く＝真のバグではない。無反応に見えた正体＝adb タップ精度＋**複数重複の一括投入時に `Channel.BUFFERED` 直列消費で同型スナックバー（Indefinite）が閉じた直後に即再表示される複合**（実機で確定再現。スワイプ dismiss も正常）。**2026-07-29 裁定＝集約を実装（上の Web取込表示是正 W1 に合流）**。一次情報＝`.claude/plans/device-verify-followup-2026-07-16.md`。
- **[読書画面] 縦書き表示モード — 実装は全フェーズ完了（P0〜P5・P2.5。P4=章送りも2026-07-17実装済み）・残は実機確認と意匠裁定のみ**: 連続横スクロール（右→左）× 自前Compose組版（ADR 0020・プラン=`.claude/plans/vertical-reading-mode.md`・P0全実測=`vertical-mode-p0-2*.md`）。P4の機構＝縦書きLazyRowの終端で未消費の横デルタを `ChapterPullConnection`（nestedScroll）で捕捉し既存 dragOffsetPx/settleSwipe へ接続・方向は横書きの鏡像（縦書きは右引き=次章）・覗きパネルも縦書き本文で描く。スパイク計測器は`android/app/src/debug/`に収載済み（P6 OPPO較正で再利用）。P4は実機体感確認済み（2026-07-17 ユーザー確認）。**残**: ①縦書き下端バー鏡像＝**実装済み（2026-07-29 裁定「配置ごと鏡像」・縦書き中は[次章←｜目次｜表示設定｜→前章]・遷移先結線までテスト固定・モックは縦書き並び未規定と確認済み＝why記載）→残は実機目視のみ**。 ②P2.5題字の目視（実機残） ③章見出しの話数ラベル分離とゴシック化（データ/トークン未整備＝要design裁定）。
- **[取込/削除・実機検証の残] 本削除時に取込元PDFも削除（Room v20・実装済み・JVM緑）**: 設計3点は実装完了＝①`books.sourceUri` 永続化 migration（v19→v20）②取込時 READ|WRITE 永続権限を本の生存中保持〔起動時掃除の keepUris に `books.sourceUri` を合流〕③`DocumentsContract.deleteDocument` の失敗を Snackbar 通知して本削除は成立。UI＝削除ダイアログの opt-in チェック（既定OFF・`sourceUri` を持つ本がある時のみ・D構造＋M/P/J全スキン共通の `DeleteSourcePdfOption`）。**実機検証 2026-07-25＝(a)(b) PASS**: (a) install -r 複数回で蔵書生存（identity hash 互換実証） (b) 捨て本で opt-in チェックの条件表示（sourceUri 無し Web 本には非表示）→実ファイル削除成功（MediaDocumentsProvider で write 権限取得）。**残**: (c) delete 失敗時の Snackbar 表示（成功パスのみ通過＝未検証・権限失効の再現機会があれば） (d) 端末永続権限上限128件の縮退実挙動（UI 警告未実装・要否は実機体感後に判断）。検証は捨て本のみ（実蔵書で破壊フロー禁止＝memory `device-verify-delegation-no-destructive-on-real-library`）。

---

## Google Play 公開準備 — 技術トラック（2026-07-19 積み込み）

> 一次情報＝`/mnt/c/Users/qingj/Desktop/project/アプリ公開戦略/`（`Google Play公開戦略.md`・`Google Play 初回公開 完全フロー….md`・`外部リサーチ実査結果_2026-07-19.md`）。
> 決定済み方針＝組織アカウント（個人事業主）／最初から API 36／スキン M/P/J は初回リリースに含めず課金実装後のアップデート目玉に温存／ブランド名は検討中（確定が applicationId・ストア素材の前置）。手続きトラック（D-U-N-S・開業届・Play Console 登録）はユーザー側で並行。

- **[裁定済み 2026-07-19] なろう縦書きPDF取込→独自描画の規約リスク＝現設計のまま公開（ユーザー裁定）**: 実査結果【A】は「本文の機械的取得＋独自表示」該当のリスク高と評価したが、ユーザー裁定＝取込は**ユーザーの明示的手動操作**であり、対象は**なろう公式機能が提供する PDF の端末内整形再表示**＝第14条23項が禁じる「自動化された手段によるアクセス・データ収集」に当たらない、との解釈で受容。残留リスク（記録のみ）: 明示安全圏（「問題としない行為」＝WebView 無加工表示）の外側である点・第14条24項の包括条項＝最終的に運営解釈次第である点。設計上の守り（現設計で既に満たす・将来も維持）: ①一括・自動 DL を実装しない ②取込までの導線は公式ページを無加工・広告込みで表示し、DL ボタン押下は毎回ユーザー（WebView 読書も選択肢として提供。※アプリ体験の主役は取込 PDF のネイティブ描画＝2026-07-19 ユーザー明言） ③外部送信なし・端末内完結。必要が生じたら企業・団体向け窓口（syosetu.com/businessinquire/）へ事前照会の選択肢は残る。
- **[SDK] targetSdk/compileSdk 36 移行 — 完了（2026-07-29・実機掃引まで含む）**。以後この便で残る注意点のみ: 16KB ページ要件は自前 `jniLibs` 無しでも Compose・datastore 由来の `.so` が 4ABI×2 入るため**依存バンプのたびに再確認が要る**（ELF `p_align=0x4000` と `zipalign -P 16` の2点。現状は合格）。また JVM テストは全ファイルが `@Config(sdk = [34])` 固定＝**targetSdk 35/36 固有の実行時挙動はテストでは一切捕まらない**（実機が唯一の検証手段）。
- **[署名/AAB] リリース署名 — 配線と署名付き AAB の生成は完了（2026-07-29）。残＝①鍵のバックアップ②実機インストール検証**: upload keystore は `/mnt/c/Users/qingj/keystores/novel-reader-upload.jks`（RSA 2048・**2053-12-14 まで**・alias `upload`・DN `CN=NovelReader Upload, O=TTA, C=JP`）。資格情報は `android/local.properties`（gitignore 済み）の `release.*` 4行から読み、鍵が無い環境では未署名で通るフォールバック付き＝別 worktree でも release ビルドは死なない。`bundleRelease` の AAB は bundletool 1.18.3 の universal APK で **v2/v3 署名・targetSdk 36・16KB 整列**を確認済み。Play Console 照合用の upload 証明書 SHA-1 = `FB:ED:C1:33:BC:1B:44:CA:35:B5:99:0D:47:6B:7C:27:B4:03:43:E3`。実機インストール検証も完了（2026-07-29・upload 鍵署名の universal APK を実機投入し、起動／Room 読み書き／なろう API 検索／作品の本棚追加まで R8 収縮後も正常。**PDF 取込経路（PDFBox）だけは SAF 操作が要るため release ビルドで未検証**＝R8 ルールを触ったときは必ず手動で通すこと）。**残: 鍵ファイルと `local.properties` のパスワードのバックアップ（ユーザー作業。紛失時は Play への鍵差し替え申請が発生）**。
- **[ID] applicationId 変更（初回アップロード前・必須）**: `com.novelreader` は公開後永久変更不可。ブランド名確定と同時に固有 ID へ変更。`${applicationId}` 参照（FileProvider 等）は自動追従・ハードコードの有無を grep で確認。実機では別アプリ扱い＝既存検証端末のデータ引き継ぎなし・benchmark の `applicationIdSuffix` 追従も確認。
- **[Play要件] プライバシーポリシー — 下書き済み（2026-07-29・`docs/store/privacy-policy-draft.md`）**。残＝①プレースホルダ確定（アプリ名/屋号〔組織アカウント公開名義と一致必須〕/問い合わせメール/公開日）②ホスティング＝**GitHub Pages に裁定済み（2026-07-29・公開専用リポジトリ分離）**＝残る作業はブランド名確定後のリポジトリ作成・公開③公開後にアプリ内からのリンク設置。※作成過程の事実訂正: 「Cookie 一切読み取らず」は不正確＝PDF 取込 DL 時のみ同一サイトへ Cookie 転送（`PdfImportViewModel.kt`）——両ドキュメントに正確に反映済み。
- **[Play要件] Data safety 申告 — 下書き済み（2026-07-29・`docs/store/data-safety-draft.md`）**。残＝提出時に実フォーム設問文言との突合のみ（Auto Backup の扱いは **2026-07-29 裁定済み＝「収集なし」で申告**・審査指摘時のみ保険申告へ切替）。AdMob 導入時の差分は §3 に整理済み。
- **[機能] In-App Review API — 実装済み（2026-07-29・JVM契約4本緑）**: トリガ＝初回読了の false/null→true 遷移のみ・セッション1回腕木・`ReviewPrompter` IF＋`PlayReviewPrompter`（クォータ超過/Playサービス不在の無表示は正常系＝ガイドライン準拠で握る旨 why コメント化）。review-ktx 2.0.2。**R8 ルールは追加が必要だった**（2026-07-29 実測で解決済み）＝「consumer rules で足りる」の見立ては誤りで、導入後の初回 `assembleRelease` が `minifyReleaseWithR8` で BUILD FAILED（Missing class `com.google.android.gms.common.annotation.NoNullnessRewrite` ← `ReviewManagerKtxKt` の SAM 変換合成クラスが GMS 内部アノテーションを参照するが、当該クラスは play-services-base 側にのみ在り推移的依存に入らない）。`proguard-rules.pro` に `-dontwarn` を1行追加して収束（why はファイル内コメント）。**残＝内部テストトラックでの実表示確認のみ**（sideload では ReviewManager が no-op＝実機では確認不能）。
- **[素材] ストア掲載素材**: アイコン 512²・フィーチャーグラフィック 1024×500・スクショ最低2枚（1枚目＝PDF ふりがな付き縦書き／本棚を文言付きで）・タイトル30字（「[固有ブランド] - 縦書き小説リーダー」型・他社商標入れない）・短い説明80字冒頭に差別化語・詳細4000字（表記ゆれ自然配置）。命名確定が前置。商標チェック＝J-PlatPat 称呼検索＋ストア同名確認＋ドメイン/SNS 可用性（手順＝実査 C-5）。
- **[フェーズ2・課金時] 特商法表記**: 買い切り IAP 販売開始時に「特定商取引法に基づく表記」が必要（実査 B-3・住所/電話の省略運用可否は専門家確認）。掲載＝アプリ内 or プライバシーポリシー同居 URL。資金決済法は買い切りのみなら非該当（B-6）。

## ★UX/Design 全層監査 — 残タスク（2026-07-12）

> **これは何か**: `/mnt/c/Users/qingj/Desktop/project/UX`（UX24層＋Design10層＋公理候補）に対し novel-reader 全体を多エージェント監査（45体・敵対的検証済み）した指摘の、**残っている作業だけ**の action list（消化したら行を消す）。
> 消化済み分の一次情報＝`.claude/plans/ux-design-full-audit-2026-07-12.md`（§A 統合報告／§B 全指摘詳細・良い点含む）＋`.claude/plans/ux-audit-batch-execution-20260712.md`（実行記録）・実装＝git log（`ui/polish`）。
> 監査バッチの実装ブランチ（ui/polish）は main 統合・worktree 撤去済み＝残りは deferred/backlog のみで専用ブランチ無し。ゲート＝`cd android && testDebugUnitTest`＋`python3 tools/check_design_tokens.py`。**意匠絡みは Compose で自己判断せず ADR0005/0014＋モック正本に先に接地**。実機絡みは PushNotification→目視OK→コミット。

### 残1: 発見帯 collapse 退避アニメ 体感の追い込み（deferred polish・ADR0005-B 実機後詰め層）
- 本棚発見帯『新しい物語を見つける』の **collapse は「完全退避」で確定・実装・実機OK 済み**（2026-07-14）。裁定の経緯＝案B「スクロール退避」の初回翻訳（下スクロールで帯を1行版へ restyle）を実機却下→**同一要素の restyle をやめ、帯は restyle せず `shrinkVertically`＋fade で高さ0へ畳んで退避・状態フィルタは sticky で常時 top に残す**へ再設計（ユーザー原則「位置/役割が同じなら restyle しない・退避のときだけ消す」）。帯＋フィルタを Lazy 外の固定ヘッダへ hoist（`LazyVerticalGrid` に stickyHeader 無し＝グリッド/リスト両モード一律 sticky の素直な解）・帯の可視は `derivedStateOf`（先頭到達＝先頭書影が最上部付近〔8dp デッドゾーン〕のみ true）で駆動。
- **残るのは退避アニメの体感が『不足』**（2026-07-14 実機・ユーザー所見・「まぁいい」で現状採用）＝終了時スナップ（slideOut の予約スペース一括除去）は shrink 化で解消したが、**閾値トリガの AnimatedVisibility（8dp 超で 150ms 縮小）はスクロールと完全連動しない**ため、退避開始のタイミングに軽い不連続感が残る。**要すればスクロール連動方式へ再設計**＝band 高さ∝スクロールオフセットを nestedScroll で連続縮小する collapsing header 本来型（duration/easing のトークン調整でも一部改善余地）。**発見は第二の柱＝いずれ本節ごと再調整予定**（ユーザー 2026-07-14）。
- 試作/裁定の記録＝`docs/design-candidates/discovery/bookshelf-band-collapse-D.html`（却下1行restyle vs 完全退避の実スクロール対比）・`bookshelf-band-tailtile-D.html`（完全退避 vs 案C末尾タイル）。原型4案＝`bookshelf-band-reposition-D.html`。ADR0005/0014 接地。

### 残3: 人間テスト（第三者便のみ残）
> 本人＋実機で可能な分は 2026-07-14 に消化済み（T1/T5/T6/T7=OK・T2=要改修→★残7⑤。結果＝`.claude/plans/usability-test-results-2026-07-14.md`・派生改修＝★残5〜8）。実機検証6件（旧残2）は 2026-07-16 消化済み＝一次情報 `.claude/plans/device-verify-6items-2026-07-16.md`（5 PASS・TalkBack没入時ナビ不達 FAIL は customActions 方式で同日是正・JVM semantics テストで固定）。残るのは**初見/時間差が本質で本人テスト不成立の2件**＝T3（二読書面の操作言語混乱）・T4（中央タップトグルの再発見。コード事実＝ヒントは通算1回きり・再表示条件なし）＋**実TalkBackの音声走査での是正確認**（uiautomator 静的確認では customActions が見えないため人間便に合流）。実施は第三者ユーザビリティテスト便で（プロトコル＝`.claude/plans/usability-test-protocol-2026-07-12.md`）。

### 残4: 監査派生 backlog（新規タスク）
- **蔵書内フィルタ/series 束ね UI**（確認バッチC④＝保留）: ロジック `filterBooksByQuery` は実装済み・UI はモック未表現のため保留（`BookshelfScreen.kt:442`／`ShelfItems.kt:37`）。series 束ねはスキーマ変更要（設計案のみ）。
- **目次の部/編 折り畳み**: 抽出パイプラインに階層データ無し＝**抽出側の新機能**。実PDF→HTML の階層有無は要検証で「フラット確定」＝畳みは前提データ欠如で現状不成立。
- **lint 残 warnings（任意改善・非ブロック）**: UsableSpace×2（`DefaultBookRepository.kt` の抽出前空き容量チェック）＝`getAllocatableBytes` は消去可能キャッシュ込みの楽観値で事前チェックが甘くなり ENOSPC で変換終盤失敗を招くため、現状の保守的 `usableSpace` は意図的。触るなら API26 分岐・例外処理込みの設計判断が要る（純機械修正ではない）。※ ModifierParameter×3 は Compose 規約準拠で解消済み。

## UI/UX 宿題

- **[読書] 表示設定の調整中はシートを半透明にして本文を見えるようにする**（2026-07-29 ユーザー要望）: 「表示設定」から文字サイズ・行間・本文余白を変えるとき、**シートが本文を覆っていて調整結果が見えない**＝何を基準に合わせればいいか分からない。調整バーを触っている間はシートを透明〜半透明にし、本文への効き目を見ながら追い込めるようにする。効き目が見えることが目的なので、シートだけでなく操作バーや薄暗幕（スクリム）も含めて「触っている間だけ引く」設計で考える。実装前に `/visual-language` を通す（HTMLモックが正本・意匠の自己判断禁止）。→ **案3「一行残し」で実装済み（2026-07-29・押下中のみシート/スクリム/上下バー全退避＋触行白ピル・160/260ms トークン化・ModalBottomSheet 継続で成立）＝残は実機目視のみ**（3スライダー各行の行対応・M/P シート面グラデも退避・離して復帰）。候補モック＝`reading-settings-livepreview-D.html`（案1/2/4 は不採用）。※作成時の発見＝正本 settings-D.html が実装に drift（「システムに従う」チップ・縦書き節・本文余白スライダー未反映）→下のモック逆同期節に登録。
- **[実機で導線が破綻・案X へ設計変更（2026-07-29）] 蔵書復旧導線**: 案B（棚バッジ→4分岐ダイアログ）＋案C（起動時一括検出→まとめて再取込）を実機投入したところ、**「まとめて再取込」が構造的に常に0冊**になり機能しなかった。**真因**＝案C は主機序を「uninstall→Auto Backup が DB のみ復元」と置きながら自動対象を①AutoPdf（**永続URI権限が生存**するPDF）と④AutoWeb に限定していたが、**uninstall すれば永続URI権限も必ず一緒に消える**（Auto Backup は URI 権限を復元しない）＝主機序では PDF 本は 100% ②へ落ちる。①が成立するのは「本文ファイルだけが消えた」稀ケースのみ。**実害**＝ユーザーは導線を使えず**欠落本を削除して新規取込し直し、読書位置・栞・追加日を永久に失った**（実機DB実測＝全7冊の addedAt が入れ直し時刻・progress も入れ直し後の記録）。導線の失敗がそのままデータ喪失に直結した実例。
  - **裁定＝案X「場所を1回教えるだけ」**（2026-07-29 AskUserQuestion）: SAF のフォルダ選択（`ACTION_OPEN_DOCUMENT_TREE`）を**1回**だけ求め、以後アプリがツリー配下のPDFを列挙して**中身のハッシュで自動照合**し一致本を復元する。判別の非対称性が根拠＝**人間はファイル名では判別できないが（なろうPDFは `n0000xx.pdf`）、アプリは中身で判別できる**。**ツリー権限は永続化**＝次回以降の欠落は「フォルダを選ぶ」すら不要な完全自動走査にでき、「①が主機序では成立しない」構造問題そのものへの解になる。実機実測の裏付け＝現行蔵書は**全冊 contentSha256 を保有**（照合が全冊効く）・sourceUrl/ncode は全冊 NULL（なろう再取得での代替は不可）。
  - **既存実装のバグ（実機実測で発覚）**: 記録済み sourceUri は全冊 `content://com.android.providers.media.documents/document/document%3A<数値ID>` 形式（MediaStore Documents Provider）で**ファイル名を含まない**。`sourceFileNameHint()` は `primary:Download/foo.pdf` 形式のみ想定のため、②ダイアログが「取込元の PDF: 1000027648」と**無意味な数値を表示する**。
  - **残**: ①**欠落本の削除は「復元の最後の機会」を消す破壊的操作**なのに通常の削除として扱っている＝削除時に復元不能になる旨を警告する設計（今回の実害の直接原因）②「index.html はあるが章ファイルが無い torn 状態」は現行 `hasContent` では欠落として検出されない＝**バッジもバナーも出ないのに読めない本**が成立する（検出定義の穴・影響範囲がバッジ/バナー/復元経路に及ぶため要裁定）。2026-07-29 の実機実測では1冊が index.html＋chap_1.html の2ファイルのみ（『「僕はアナベルと結婚するから…』＝短編なら正常のため異常と断定していない。裁定の前にこの本が短編かの確認が要る）③Service 経由の復元完了通知が「変換完了」文言のまま（1行分岐候補）④バッジのスキン別表出は K/D のみ（M/P/J 翻訳はモック未裁定＝スキン後回し枠。ダイアログ動線自体は route 層差し替えで全スキン有効）。
- **[2026-07-29 裁定待ち] 発見ホームのスクロールアンカー崩壊＝残る2系統**（機序の正本＝knowledge `lazylist-loading-full-replace-scroll-reset`。K のランキング初訪ページは同日修正済み・L1/L2 の検知も導入済み＝git log 正本）:
  - **①D/C・M・J・P に残る別トリガの同型**: これらは単一控えを期間跨ぎで流用する形なので「初訪ページ」は存在せず今回の穴は無いが、**process death 復帰／VM 再生成で「控えが無い Loading なのにスクロール位置だけ復元される」場合**は同じ機序（Loading の全置換→総コンテンツ高の崩壊→先頭クランプ）で落ちる。塞ぐには4スキンの初回ロード表示を spinner/status から骨（`RankingListSkeleton`）へ変える必要があり、これは `DiscoveryHomeContentTest` に明記された既存裁定「初回は骨格を出せない＝進捗インジケータで待つ」の変更＋4スキンの可視変更を伴うため据え置き。**やるなら一括**（D/M/J は行が LazyColumn の item なので `items(RankingSkeletonRowCount)` 化が要る＝スクロール index の復元まで効く）。
  - **②結果一覧(Result)系4実装**: 同機序だが「新クエリでは先頭表示が正」という解釈があり得るため未裁定のまま。L2 メタテストの `acknowledgedOutOfScope` へ**理由付きで登録済み**（黙って検知対象から外していない）＝裁定して「位置を保つのが正」となったら登録を外し、不変条件の対象へ移す。
- **[モック逆同期・2026-07-16 棚卸し]** 一次情報＝`.claude/plans/mock-drift-inventory-2026-07-16.md`（正本モック全数の未反映リスト・優先順位・恒久ルール）。注記済み＝fusion-D（発見帯の完全退避構造のみ未反映⚠️・全面描き直しは★残1の方式確定後。栞ランダム先端/棒長 `f208608`・取込バナー `ProcessingBanner` は 2026-07-16 に fusion-D／multiselect-D へ逆同期済み）／bookshelf-D（旧世代＝退役・提案基盤に使わない）／**settings-D（2026-07-29 検出）＝実装に対し3点未反映**（「システムに従う」チップ・縦書き節・本文余白スライダー）＝逆同期候補。**恒久ルール**: コード先行の視覚変更を入れたら正本モックへの逆同期 or「未反映」注記をセットで／モックのプレビューは必ず `mockview`（素の `chrome <file>` 禁止）／**本棚を下敷きにする候補モックは既定スキンの現行正本（`skins/bookshelf-K.html`）を下敷きにする**——旧世代（bookshelf-D 直下系・fusion-D）は語彙参照のみ可・構造下敷き禁止（2026-07-29 の復旧導線候補が旧基盤で描かれ差し戻しになった実害。委譲時は下敷き正本のパスを仕様に明示する）。
- **[モック追従・構造] 発見系モックの情報/装飾テキスト再分類**（2026-07-12 `a9a6a5c` 実装時に留置）: `InfoText` トークン（実装済み＝発見系の情報メタ6箇所を AA(4.5:1) へ引き上げ・Light #5C606D／Sepia #6C6148／Dark #8A929B）の discovery/*.html モックへの追従は、`--ink-soft` を共有する **10〜16箇所/ファイルの情報・装飾テキストの個別再分類**＋`--info-ink` 変数の新設＋`tools/check_design_tokens.py` へのマッピング追加が必要＝構造的大改修と判定し留置。**現状の一致検査は InfoText を未トラッキングで PASS＝この層ズレ（コードが AA へ引き上げた面をモックが `--ink-soft` のまま持つ）は未検知**になる点に注意。

## なろうAPI 発見・検索機能（第2の柱）

> Phase 0〜4 完了（現況＝`STATUS.md` §0・実装＝git log）。目標ロードマップ・作る機能一覧の一次情報＝plan `~/.claude/plans/api-agy-woolly-swan.md`。監査残課題（構造系）は下の「リファクタ / 技術的負債」。

## リファクタ / 技術的負債（deferred）

- **[2026-07-27 リファクタ大バッチの残り]**（消化分の正本＝git log・裁定と依存グラフの一次情報＝`.claude/plans/refactor-batch-2026-07-27.md`）:
  ③ Baseline Profile 生成＝**見送り裁定（2026-07-27）**。profileinstaller/macrobenchmark/StartupBudget は揃っており generator 1本で起動20〜30%改善見込み・要実機
  ④ 計測・調査群＝**見送り裁定（同）**: ShioriCover の Path 毎フレーム確保（drawWithCache 候補・BookshelfScrollBenchmark が予算内なら実害なし＝先に測る）／OkHttp ディスクキャッシュ未設定／Room AutoMigration 不使用の方針 ADR 1行／Native 接頭辞・ビュー切替名の整理
- **検索画面 S3＝カテゴリ列の LazyColumn 化（保留・要否判断）**: 重さの正体は「カテゴリ展開状態での操作毎の全画面再コンポーズ」で、S1（選択判定 Set 化・Regex 定数化）/S2（strong skipping＋@Immutable）は解消済み・実機体感は軽快（2026-07-11 実測）。残る理論コスト＝非 Lazy Column 上の22カテゴリ/115チップ（`DiscoverySearchScreen.kt:203-207`）の画面外存在コストと「全展開のまま再訪」の初回構成。**体感問題が再報告されるまで保留が妥当**。
- MigrationTest が「16.json 形状（web_reading_progress 無し）→17」経路を構造的に検証できない（chain テストは 14→15 でテーブルが生まれる系譜のみ通過）。既知の実機 v16→v17 未検証と同根の coverage-hole として記録。

## workflow / tooling

- **[bestpractice 突合の回収候補（2026-07-12 調査）]**: ①`block_destructive_migration.py` の Bash 経路が素朴な部分文字列一致（`FOO=1 cmd`・`$()` ですり抜け）＝settings permissions の `if` フィールド化を検討（主経路の Edit/Write 捕捉は健在で実害小） ②サブエージェントの部品別モデル配分（fan-out/読み=haiku・照合=sonnet・監査=opus。現状は env `CLAUDE_CODE_SUBAGENT_MODEL` で opus 固定＝見直しは settings 変更を伴う）。※旧①`triggers:` は 2026-07-16 検証済み＝標準外と公式ドキュメントで確定し、全8スキルを公式 `when_to_use` へ移行済み。

- **antigravity-delegate サブエージェントの同期実行が保証されない**（2026-07-07・委譲5件中3件で再発): agy をバックグラウンド起動したまま「待機中」で終了し完了通知が来ない。プロンプト明記・SendMessage 再開でも再発。運用回避（CLAUDE.md 委譲判断節に反映済み）＝完了判定を報告でなく**成果物の存在**（`git status`/grep/`ps`）で行う。**根治候補**＝プラグイン側で agy 起動を同期実行へ強制するか wrapper にポーリング内蔵。優先度中（運用回避が効き非ブロッキング）。※2026-07-26 にプラグインごと無効化したため当面は発生しない＝**agy 解除時に再燃する宿題**として保管（CLAUDE.md 委譲節）。
- **worktree(ext4) 作業の冒頭で `gw :app:lintDebug` を回す運用**: Lint の自動コミットゲートは現存しない（かつての canonical ローカル hook `check_lint_on_commit.py` は撤去済み・git 未追跡＝そもそも task_diary #419 のとおり導入以来 fail-open で一度も走っていなかった）＝lint の担保はこの手動スイープのみ。ext4 worktree なら in-tree で回るので冒頭で1回スイープする。基準＝0 errors/31 warnings（2026-07-18 時点＝+3 は新規スキン/bench ファイル由来の非ブロック警告。ModifierParameter×3・UsableSpace×2 は従前どおり意図的）。

- **[2026-07-29 裁定・未着手] 既知バグレジストリ＝再発防止 L4**: 修正済みバグを ID 化し「検知手段（不変条件テストID／lint／なし）」を紐付けて台帳化し、**無防備な知見を可視化**して `/stale-check` で棚卸しする。目的は投資判断（どのバグ型に検知を足すべきかが一目で分かる）。前提＝L1（個別回帰テスト→不変条件テストへ格上げ）・L2（ソース走査で新実装の取りこぼしを検知するメタテスト）は実装済み。L3（knowledge へ `triggers:` を持たせ Edit/Write 時に該当知見を自動注入するフック）は **L4 の可視化結果を見てから採否を判断**する。※ L2 が要る理由＝knowledge も回帰テストも在ったのに K のランキング Pager 化で同じ機序を再発させた実例（既存実装の退行は防げても新規実装が同じ罠を踏むのは防げない）。
- **[較正待ち] 委譲粒度の谷=30 の実測較正**: 委譲ターン計測フック（`count_delegation_turns.py`・2026-07-25 新設＝
  30/60/90…回で子へ中間通告＋完走時 `~/.claude/projects/...-novel-reader-andloid/delegation-stats.jsonl` へ記録）の
  分布が貯まったら（目安20〜30件）谷の位置を確かめ、orchestration §0 の「~30」を実測で更新する。

## 実行捏造検知器（ADR 0006）残タスク

> エンジン＝`.claude/hooks/detect_fabricated_execution_core.py`。完了分は **ADR 0006（増補含む）と git log が正本**。以下は開きのみ。

- **Tier B 汎用主張の免罪の限界**（事象D）: 「セッション内に成功実行が1回でもあれば免罪」で後半の汎用捏造を取りこぼす。具体値主張は具体照合に絞ったが汎用主張の掘り下げは将来課題。**進捗（2026-07-11・増補6）**: Tier E カテゴリ別突合が**現ターン分**の同根系列を吸収した。**過去ターンの汎用主張の掘り下げは引き続き将来課題**（Tier E は現ターン境界での判定）。
- **[2026-07-11・増補6 保留設計] 案3＝委譲主張の E2 突合（opt-in）**: 「〜を委譲した／agy に生成させた」等の委譲完了主張を、**委譲先 transcript（`subagents/agent-<id>.jsonl`）の tool_use とカテゴリ突合**して裏取りする案。**真陽性サンプルが皆無のため保留**（設計要点のみ保全）。真陽性が観測されたら着手。
- **[2026-07-11・増補6 残課題] D5 対象語突合の字面依存FPクラス**: D5 は帰属対象の名詞（違和感/懸念/指摘…）の**字面**を実入力に探すため、ユーザーが真に指摘したが当該名詞を書かなかった場合「あなたの指摘は的を射て」型が潜在FP化しうる（現コーパス実測 0件・非ブロック Tier D で被害限定）。同義語・意味突合の導入は将来課題。
- **[2026-07-11・増補6 で切り出し] Tier E の Stop 昇格の再判断**: 新既定 ABCDE での CLI 運用実績（真陽性の積み上がり・FP 率）が揃ったら再判断。昇格には conf 設計の引き上げ（現 0.55-0.7 → Stop 閾値 0.8）または Stop 側の per-rule 閾値の新設計が必要。
- **意味照合系検知器**（着想段階・スコープ外構想）＝生成コード不具合・外部リサーチ捏造（正解データ事象B/C）。

## D. 長期・品質（backlog）

- **超長編抽出エッジ残差の③アポストロフィ座標順**（N6169DZ・章題ドリフト残2件）: `兎'ｓ`↔`'鳥…` の座標順ずれで**1:1コードポイント置換不可**＝実質 won't-fix。基準＝`ab-review/golden_regression`、詳細＝task_diary #35。

## A2. UIスキン機構（M/P/J 統合済み＝main／残＝C 夜行・将来送り）

> 2026-06-27 の「まだ実装しない」を 2026-07-17 ユーザー指示で解除。機構の裁定＝ADR 0021・フェーズ詳細＝`.claude/plans/ui-skin-framework-2026-07-17.md`（P1 骨格は完了＝git log 正本）。

- **スキンモック確定（2026-07-17 完了）→ M/P/J Compose 実装（完了・main 統合済み）**:
  - **確定＝実装対象3スキン・全画面モック収蔵済み**: M 星図・P カートリッジ（目次=はっちゃけ版採用）・J ポータル・デッキ。正本= `docs/design-candidates/skins/{bookshelf,reading,discovery,toc,settings}-{M,P,J}.html`（機能はD正本と全数突合済み・ダイジェスト§B検収済み・座標監査済み）。裁定の経緯・審美規範=ADR 0021 追記（2026-07-17）＋auto-memory `feedback-skin-design-judgment-criteria`・生成規範=`.claude/plans/skin-design-digest-2026-07-17.md`。
  - **保留**: Q 読書の庭＝差し戻し保留（「改善ポイントが多すぎる」。最終版は `skins/candidates/*-Q.html` に保全）／候補 L/N/O/R/S＝本棚1枚のみ（同 candidates/）／P はっちゃけ試作の不採用4画面= `candidates/hatchake/`。旧A〜J原本は claude.ai/design `ui-n-phase0/`。
  - **M/P/J Compose 実装＝完了・main 統合済み**（全5画面×3スキン・C3 実機スモーク済み＝STATUS §0／git log 正本。一次情報＝`.claude/plans/skin-compose-implementation-2026-07-17.md` 末尾「実行状態」節）。前提の設計判断＝スキン機構を「トークン束＋画面構造」の二層へ拡張（ADR 0021 追記2）。
  - ツール: 候補比較= `tools/build_skin_gallery.py`・画面別ギャラリー（等倍・スキン単体可）= `tools/build_screen_gallery.py [ID]`。プレビューは必ず mockview。DesignSync は主セッション限定。任意: claude.ai 側への収蔵バックアップ同期は未実施（必要なら finalize_plan 経由）。
- **⚠️ C 夜行の欠落モック初稿は方向不一致で差し戻し（2026-07-18）**: 発見/目次/設定＋一覧2案のドラフトを生成・提示したが「全然違う」（詳細未聴取のまま「今は既存を詰める方向で」の指示）。ドラフトは `candidates/c-draft/`・`candidates/list-C/` へ退避（git追跡済み・2026-07-25 棚卸しで「未追跡」記述を是正）。**再着手時はまずユーザーの夜行イメージの言語化（何が違ったか）から始める**こと（憲法2枚＝bookshelf-C/reading-C の読み込みだけでは不十分と実証された）。
- **C 夜行の「らしさ本体」＝構造・演出層（別タスク・都度追加。2026-07-17 実機比較で再裁定）**: P3 色トークン層は実機で C 適用を確認したが、モックの体感は〈本棚=続きからヒーロー＋静かな1列リスト・栞書影の C 用ミュート・ember の効かせどころ（章番号エブロウ/上端ヘアライン進捗/続きからラベル）・極小クローム＋読書の浮きピル〉の構造・演出層に宿っており色だけでは別物に見える（ユーザー実機確認済み）。**着手はモック作成（発見/目次/設定の C 版含む欠落分）から**＝スキンごと構造切替の枠は P2 の外枠に含めず、実装は画面単位で都度。⚠️ **人間の実機目視で「これが夜行だ」と確認できた事実はまだ一度もない**（2026-07-17 の実機スモークは Claude の screencap による「C トークンが適用されている」確認のみ。人間が見た印象は「夜行に見えない」＝色層だけでは D ダークと区別がつかない）。構造・演出層の実装後に改めて人間目視の関門を通すこと。
- **将来送り（ADR 0021）**: 栞「型」軸（A箔/C小口/D蔵書印/E綴じ紐）／D 以外のテーマ変種／旧候補の移植（J は 2026-07-17 に実装対象へ昇格済み・I は退役のまま）。A〜J 資産は claude.ai/design（プロジェクト `Novel Reader UI`・projectId `bb5a35c8-70ac-4efa-bb03-1579d3f11d93` の `ui-n-phase0/`・`DesignSync: get_file` で再取得可）に保持。bookshelf-D へのセピア変種追加も引き続き再検討枠（現状は `SepiaColorScheme` が本棚セピアの正本）。
