# handover — やること台帳（main）

> **次に何をやろうか悩んだら、まずここを見る。**
> 作る予定のもの・あとで拾う思いつき・その場から漏れた取りこぼしを書き溜める場所。
> 思いついたら「思いつき・取りこぼし」へ追記して育てる。
> **今どうなっているか**（状態・完了・既知不具合）は `STATUS.md`。一次情報の細部は `.claude/plans/` のアーカイブhandover。

## 思いつき・取りこぼし（随時追記）

> レビュー中・実装中に出た宿題や着想で、まだ正式バックログに整理していないものをここへ。
> 育ったら下のA〜Dへ移す。

- **[実機/最優先] 2026-07-08 統合ツリーの実機スモーク（統合後まだ一度も実機で動いていない）**: 次の実機作業の最初にやること。①発見系5画面＋PDF↔Web継続読書＋変換の停止/再開の統合スモーク ②v9→v10 migration の実機通過（起動即クラッシュ監視＝task_diary #39 追補の機序） ③resilience（applicationScope）× FGS スコープ再生成 fix（`810f2dd`）の共存動作。**特に注意**: 発見UIの実機検証は全て旧 BOM（material3 1.2.1）時代のもので、統合により BOM 2025.02.00（material3 1.3.1）が発見UIへ初適用＝ModalBottomSheet を多用する条件シート・紐付けシートは挙動/見た目が変わりうる層（スライダー意匠が変わった前例＝§B）。
- **[品質/lint] 統合1万行の Android Lint 初回スイープ完了（2026-07-08・ext4 一時worktreeで実施）**: **0 errors / 21 warnings**。新規コード起因の実質指摘は2件のみ＝①`PdfProcessingService.kt:123` InlinedApi（`FOREGROUND_SERVICE_TYPE_DATA_SYNC` は API 29+・minSdk 26＝API 26-28 での FGS type 挙動は未検証） ②`DiscoveryCommon.kt:163` ModifierParameter（modifier が最初の optional 引数でない＝上記系統2/3 と同ファイル・リファクタ時に同時解消）。残りはノイズ＝GradleDependency 12件（新版通知・意図的据え置き）／外部jar bouncycastle 3件／積年リソース系（DataExtractionRules・MonochromeLauncherIcon×2・ObsoleteSdkInt）。**構造メモ**: Lint コミットゲート（`check_lint_on_commit.py`）は drvfs でスキップされる設計＝canonical 作業が続く限り事実上無効 → **worktree（ext4）作業の冒頭で `gw :app:lintDebug` を回す**のを運用に組み込む。
- **[テスト投資] Room Migration の instrumented テストが存在しない**（androidTest は pdf/ と ui/ のみ）: identity hash 起因の起動即クラッシュを実測で2回踏んだプロジェクト（task_diary #39）＝「実機で踏む」を「テストで捕まえる」に変える価値が高い。`MigrationTestHelper`＋既存 `android/app/schemas/3〜10.json` で「7→8→9→10 の全パス＋fresh install」を検証する androidTest 1本。次の実機作業（上の統合スモーク）と同便で書く・回す。
- ~~**[統合判断・AB後処理] `api-lab-ai`（Fable版・ブラインド勝者）vs `api-lab-ai2`（Opus版）の採否**~~ → **解消済み（2026-07-07・実測台帳＝`../claude-bestpractice/models/experiments/2026-07-07-ab-test-opus-vs-fable/`）**: 審査6ラン全会一致で Fable 版を採用し現 `api-lab-ai` 系統へ一本化・Opus 版ブランチは破棄（地雷だった Opus 側 task_diary #42/#43 と誤事実記述もブランチごと消滅）。優位点の移植は ① セピア値のモック同期＝下記「reading-D モックへセピア新値を逆反映」として存続 ② キュレーション語のカテゴリ横断一意テスト＝移植済み（`NarouCuratedKeywordsTest`）。
- **[品質/宿題] chrisbanes-skills 全体レビュー残指摘（2026-07-08・api-lab-ai-2・6観点並列レビューの未対応分）**: 実挙動バグ2件（ステータスバー復元・Service死スコープ）＋コメント訂正＋読書ホットパス skippable 化は同日対応済み（コミット表は STATUS）。以下が残り。機会リファクタ時に該当ファイルを触るならついでに拾う:
  - **系統1・画面 Composable の VM 直結**（state-holder-ui-split／テスタビリティの両観点が独立に同根指摘）: `BookshelfScreen`/`NativeReadingScreen`/discovery 4画面が「VM受取＋collectAsState＋全レイアウト」1関数で、`state`+callback のみの stateless 版が無い。VM 側は JVM で厚くテスト済みなので、分割すれば描画側テストの土台が揃う。優先画面から段階分割。関連: `NcodeLinkSheet.kt:76-117` は `NovelApiRepository` を直接受け `produceState` で検索まで回す＝依存漏れの最たる例。
  - **系統2・modifier パラメータ欠落＋root サイズ固定**: `BookCard.kt:45`(BookProgressRow)・`ProcessingBanner.kt:27,83`(EmptyBookshelf/ProcessingBanner の fillMaxSize/fillMaxWidth 内部固定)・`DiscoveryCommon.kt:165`(DiscoveryStatusBox の fillMaxSize 固定＝呼び出し側の要求が実際に分岐しており顕在)ほか低優先（DiscoveryHomeScreen セクション部品・DiscoverySearchScreen チップ群）。
  - **系統3・DiscoveryStatusBox の flag-soup** `DiscoveryCommon.kt:158`: `isLoading+emptyMessage?+errorMessage?` の3形状切替。呼び出し側は sealed `DiscoveryUiState` を持つのに損失変換して渡している→sealed 直受けへ。
  - **系統4・primitive obsession**: `ncode: String` 全域生 String（`linkNcode(bookId: String, ncode: String?)` は取り違え無防備）→ `@JvmInline value class Ncode` の教科書的候補（Room/Moshi 境界は生 String 維持の段階導入）。`saveScrollPosition(bookId, filename)` の String 連続も同類。
  - **系統5・検索シートのカスタム文字数/読了時間が二重真実源** `DiscoverySearchScreen.kt:459-514`: この入力だけ画面 remember＋LaunchedEffect で VM draft と双方向同期＝SearchDraft(VM保持)の設計と不整合。draft 側へ畳む。
  - **中低優先の粒**: `lifecycle-runtime-compose` 導入で `collectAsStateWithLifecycle` へ（現状は WhileSubscribed(5s) で緩和済）／`NovelDetailViewModel.load()` に loadJob キャンセル（後着レース。Discovery 側は対策済みの非対称）／`DiscoveryViewModel.kt:103,123,190` の `.value=copy` → `.update{}`／`DiscoveryResultScreen.kt:137-148` ループ内 remember に `key(label)`／discovery 2画面の LazyColumn に `key={n.ncode}`（本棚が手本）／`ChapterContent.kt:162` TextStyle 毎回生成・`Theme.kt:40` colors 毎回アロケートの remember 化／フォントスライダーの永続化を `onValueChangeFinished` へ／`NativeReadingScreen.kt` snapshotFlow の `onSaveScroll` を `rememberUpdatedState` で防御／compose compiler `reportsDestination` 追加（skippability の機械検証手段がゼロ）。
  - **テスト投資（compose-ui-testing-patterns 観点）**: ①最大ROI＝葉 Composable（`ReadingErrorScreen`/`DiscoveryStatusBox`/`NativeTableOfContentsScreen`/`ContinuationCard`/`ReadingSettingsSheet` 等）は既に state+callback の理想形なのに UI テスト0本＝リファクタ不要で分岐描画・callback 結線テストが書ける ②実行環境の意思決定: (a)Robolectric 導入で JVM 実行 vs (b)既存思想どおり androidTest（ROI なら a が有力） ③`@Preview` は RubyText の3本のみ→プレーン部品へ分岐ごとに追加（ほぼゼロコスト） ④`calculateRubyPositions`（ルビ行またぎ位置）の Instrumented/screenshot テスト1点導入（RubyLayoutHelperTest.kt 冒頭コメントの訂正と連動＝かつて「Instrumented で別途検証」と書かれ実在しなかった）。スクショ全面導入は ADR 0005 §B のスコープ外方針を尊重し非推奨。

- **[UI/宿題] 条件を調整シートの高速フリック「枠オーバーシュート」＝最終形は枠固定・スッと止まる／残件は"枠固定のまま内容だけ弾ませる"**（2026-07-08・api-lab-ai-2 で対応）:
  - **症状**: 「条件を調整」シート内を高速で上下フリックし手を離すと、慣性で**シート枠全体が Expanded 上限を超えて上（下フリック時は下=dismiss方向）へオーバーシュート→復帰**し、上方向では下端に裏画面（本棚）が一瞬覗く。中間落下と高速フリックのアンカー飛び越えは `1753211`（`skipPartiallyExpanded=true`＋`imePadding`）で解消済み＝**sheetState は正しいので触らない**。
  - **真因（実バイトコード照合で確定）**: 内容ピクセルは漏れていない（`verticalScroll` は foundation 1.6.6 の `clipScrollableContainer` で上下端 top=0/bottom=height を**厳密クリップ**・かつ `overscroll` の外側／`Surface(shape=ExpandedShape)` も角丸クリップ）。真因は **`ModalBottomSheet` 内蔵の `ConsumeSwipeWithinBottomSheetBoundsNestedScrollConnection` が内容スクロールの境界フリング残速度を `onPostFling`→`anchoredDraggableState.settle` へ渡し、そのバネアニメが minAnchor を超えてオーバーシュートすること**（material3 1.2.1・`ModalBottomSheet_androidKt`／`SheetDefaultsKt` を逆コンパイルして確認）。
  - **現状＝未修正・不具合残置（ユーザー判断で全修正案を撤去し、コードは素の ModalBottomSheet+verticalScroll のまま）**。以下は再挑戦の出発点として保全。**やること（本命）**: 「**上部の枠は固定のまま、内容だけ自然に弾ませる**」を実現する。ただし ModalBottomSheet 内蔵接続が境界フリングを必ず奪う仕様上、内容の overscroll 伸び（`velocityLeft - consumedPost`）は 0 になり素直に両立しない。真の解は**内容スクロールの overscroll/ディスパッチをシート内蔵接続から分離**する（`verticalScroll` を独自 `overscrollEffect`＋分離 dispatcher の `scrollable` に置換等）＝踏み込んだ実装。要 UI-n 意匠確認（静けさ意匠との整合）。
  - **検証済み候補と実機評価（棄却理由つき・再試行の無駄防止のため保全）**: (A) 自前 `NestedScrollConnection` を `verticalScroll` の親に割り込ませ **`onPostFling` を上下両方向消費＋`onPostScroll` 下方向消費**→**枠は 1px も動かせる（固定成功）**が、速いフリックの端で内容の弾みが消え「スッと止まる」＝ユーザー評「完全に消すのは使用感を損なう」→**撤去**。 (B) (A) に加え**自前 overscroll へ `applyToFling` で残速度を流し内容を弾ませ枠固定**（本命の簡易版）→ビルド可だが実機で意図した感触に届かず→**撤去**。 (C) **`scrimColor=background`** で覗きを覆う→「**背景に色の違う白シート**があって違和感」（シートの `tonalElevation` と地色のわずかな差で二枚重ねに見える）→**撤去**。 (D) **シートを下方へ拡張**して隙間を覆う→ModalBottomSheet は内容が縦スクロールだと Surface 高を fullHeight でキャップ（スクロールが超過分を吸収）するため**画面下端より下へは伸ばせず不可**。 (誤) `onPostFling` を**下方向だけ**消費→「変化なし」（症状は上方向のため誤方向）。
  - 一次情報: 逆コンパイル手順・クリップ矩形・内蔵接続の `onPre/PostFling` 挙動は本セッションで確認済み（`clipScrollableContainer` Vertical=`Rect(-30dp,0,w+30dp,h)`／`overscroll(clipScrollableContainer(...), effect)` の順＝clip が外側）。
  - ※ 2026-07-08 の main 統合で Compose BOM 2025.02.00（material3 1.3.1・foundation 1.7系＝`bcb5216`）へ更新された。上記の逆コンパイル知見は material3 1.2.1／foundation 1.6.6 時点のもの＝再挑戦時はまず 1.3.1 での現象再現と内蔵 NestedScrollConnection 実装の差分確認から始めること。

- **[UI/宿題] reading-D モックへセピア新値を逆反映**（2026-07-07・検索UX第2ラウンドで発生）: 実機フィードバック「ライトとセピアの色味に差がない」を受け、`Theme.kt` の `ReadingColors.SEPIA` をモック `.t-sepia` 写経値（#F3ECDD 系）から琥珀紙（bg #F2E7CE・墨 #3D3121・彩度約15%）へ意図的に逸脱させ、Material 側にも `SepiaColorScheme` を新設した（`ccca2fa`・`fa24366`）。**現状はコード側が正本**。claude.ai/design の `ui-n-phase0/reading-D.html`（.t-sepia）と、必要なら bookshelf-D にもセピア変種を逆反映してモック正本性を回復する（DesignSync 経由・任意・非ブロッキング）。

- **[UI/宿題] discovery-search-D モックを現状機能とすり合わせ → 「キーワードから選ぶ」を採用案Bの意匠へ**（2026-07-07・検索UX ⑤ 大分類アコーディオン化で発生）: 「キーワードから選ぶ」（`NarouCuratedKeywords` の基本5＋ジャンル別17＝計22カテゴリ）の折りたたみを Compose に実装済み（`9ba5049`：`category.title` キーの `mutableStateMapOf` で開閉・既定は全畳み・`CollapsibleCategoryHeader` を新設。`SectionHeader` はシート側9見出しで流用のため無変更）。ただし**この節はモック `ui-n-phase0/discovery-search-D.html` に存在せず、mock 無しで Compose に足された**＝意匠が定まらず「畳んだ見出しが均一でタップできると伝わらない／畳むと選ぶ手掛かりが無い」実機フィードバックの根本原因（＝UI-n の『見た目＝モック正本』ワークフローを外れた箇所）。加えて同モックの**条件調整シートも現状とズレ**（モック＝期間／属性／各種スライダー ↔ 実装＝更新された時期／テーマ〔含める〕／除外する／文字数等はステップチップ＋カスタム入力）。**やること**: ①`discovery-search-D` を現状機能へすり合わせ（キーワード節の新規追加＋条件シートの実装追従）→ ②キーワード見出しを**採用済みの案B**（ヘアライン一覧の「開ける行」＋濃色見出しで節ラベルと差別化＋畳んだ状態でも代表語を淡色プレビュー＝選ぶ動機を残す）へ意匠更新 → Compose へ翻訳（現状の Compose は機能のみで案Bの2段プレビューは未反映）。**なぜ分離**: モック自体が現状を反映しておらず、意匠の正本を先に整えないと翻訳の拠り所が無いため。今回モックへ一度 push した案B節は**元へ巻き戻し済み**（モックは現状 original 状態）。案Bは実機スクショ起点で比較選定済み（一次情報は本セッション）。**あわせて（2026-07-08 追記）**: ①発見系モックの**一次正本はリポジトリ内 `docs/design-candidates/discovery/discovery-search-D.html`**（claude.ai `ui-n-phase0/` の同名は収蔵コピー＝CLAUDE.md UIルール・STATUS-api-lab「意匠モックの置き場所」）→ すり合わせはリポジトリ側を編集し、DesignSync で収蔵側へ同期する。②同タイミングで `DiscoverySearchScreen.kt`（統合後 約1250行＝リポジトリ最大の god file）の条件シート部を `SearchConditionSheet.kt` へ抽出する（STATUS-api-lab 監査残課題2。本棚・読書画面の 2026-07-02 god file 分割と同じ純移動パターン＝どうせ触るときに一石二鳥）。

- **[workflow] plan モード中の read-only 探索を agy へ開放（2026-07-06 実機検証で確定）**: 「plan 内で agy は不可」は誤りだった＝plan モードはプラグイン subagent の権限層へ伝播せず、`--yolo` 無しの `agy-delegate` は plan 中でも実行され書き込みゼロ（根拠 task_diary #40／運用ルール CLAUDE.md「委譲判断 / plan運用」節（plan 中の agy 委譲は read-only／`--yolo` 厳禁））。**運用**: 〈ユーザー明示〉or〈break-even 超の read-only 探索〉は agy へ、割に合わない小さな読みは Explore にフォールバック。**plan 外での agy 探索**は「書き込みを伴う探索/生成」を plan の read-only 保証から切り離したいとき用の選択肢として残す。派生タスク:
  - ~~**[調査/hardening] plan 中 `--yolo` の機械 deny 可否**~~ → **実装・検証済み（2026-07-06）**: PreToolUse payload に `permission_mode` が露出し、plan モードでは subagent の Bash payload にも `"plan"` が届くことを実機確認 → plugin hook `validate-delegate-bash.sh`（git-test source＝live）に「`permission_mode=="plan"` かつ引用除去後 command に `--yolo`/`--dangerously-skip-permissions` を含む委譲を deny」を追加。単体8/8＋**--yolo 不使用の再検証（2026-07-06）**で end-to-end 立証（source-live／`permission_mode='plan'` 実測＋静的8/8 の合成・task_diary #40）。**⚠ カバー範囲**: この deny は **antigravity-delegate サブエージェント経路のみ**（hook が agent_type で自己スコープ）。**メインセッション直叩き・`/antigravity:delegate` slash（書き込みタスクに --yolo を明示する設計）は対象外**＝plan 中でも止まらない → primary の担保は依然「--yolo 不使用の規律」。**残**: git-test の gate commit は済（`bee18ad`）。cache(0.15.1) は gate 未搭載だが実行時は `CLAUDE_PLUGIN_ROOT`→source が live のため**動作に影響なし**＝`/plugin marketplace update`＋version bump は整合維持の hygiene（任意・非ブロッキング）。
  - **[任意・別レバー・未着手] Explore→Haiku フォールバック**: `~/.claude/agents/Explore.md`（Linux 側 ext4・Windows 非共有）に `model: haiku`。v2.1.198 以降 Explore はメイン(Opus)継承で高くなるため、agy に出すほどでない小さな plan 探索を安く回す。built-in Explore を user agent で上書きできることは公式 docs で確認済み。※ブランチ不変の Linux ローカル設定だが「採否メモ」はブランチ追従の handover が置き場。
- **[workflow/plugin hardening] antigravity-delegate サブエージェントの同期実行が保証されない**（2026-07-07・api-lab-ai 委譲5件中3件で再発）: agy をバックグラウンド起動したまま「待機中」の中間報告で終了し完了通知が来ない。プロンプトに「同期実行して完了まで待て」を明記しても・SendMessage 再開でも再発。**運用回避（現状・CLAUDE.md 委譲判断節に反映済み）**＝完了判定を報告でなく**成果物の存在（`git status`/grep/`ps`）**で行う。**根治候補**＝プラグイン側で agy 起動を同期実行へ強制するか wrapper にポーリングを内蔵する改修（正史は git-test source＝memory [[antigravity-plugin-cache-vs-source-cost-gate]]）。優先度中（運用回避が効いており非ブロッキング）。
- **[UX/進捗] 本文処理の進捗にページ数もリアルタイム併記したい**（2026-07-06 実機目視でユーザー要望・優先度低）: 現状は「本文を処理しています… X%」の通し%表示（`742a784` で読み込み/抽出を統合・**%表記はユーザー評価「完璧」**）。ページ数 n/m も併記できれば体験がさらに良い。**設計注意（重要）**: load(全ページ)と process(本文ページ=全−4)は分母が違うため、単純に2カウンタを併記すると「一度満ちて2周目に入った」錯覚が再発する（今回まさにそれを統合で解消した）→ 単一の連続ページ尺（load+process を通した実処理ページ数）にするか、%を主・ページを副に添える等の見せ方を要設計。実装点は `PdfBookExtractor` の step-1 phase 文言（＋必要なら `ProcessingBanner`）。
- **実行捏造検知器（`feat/exec-fabrication-detector`・2026-07-06 main 統合済み）の残タスク**（ADR 0006・エンジンは `.claude/hooks/detect_fabricated_execution_core.py`）:
  - ~~**Stop フックのライブ化**~~ **完了（2026-07-06 統合）**: `stop_guard_fabrication.py` を実装し `.claude/settings.json` の `Stop` に配線済み（last_turn の Tier B `confidence≥0.8`・非降格のみ `decision:"block"` で自己修正を促す）。※作業ブランチは全て main へ統合・削除済みのため「全作業ブランチへ配布」は不要化。
  - ~~**misread 型（事象F）の盲点**~~ → **解消済み（2026-07-07・`meta/hallucination-detector-tune`＝Tier C 新設）**: ペアは在るが報告が実結果と食い違う5事象を静的検知（C1 ブロック済みコミットの完了報告／C2 出力シグネチャ捏造／C3 書き込み完了捏造／C4 ブランチ削除捏造＋A2 の証拠 result 層化・エコーバック除外・時系列照合）。正解データF 5/5 検知・全106セッション走査で偽陽性ゼロ・Stop フックへ C 全4ルール＋A2 を昇格配線。詳細は ADR 0006 増補。
  - **Tier B 汎用主張の免罪の限界**: 「セッション内に成功実行が1回でもあれば免罪」で後半の汎用捏造を取りこぼす（事象D）。具体値主張は具体照合に絞ったが、汎用主張の掘り下げは将来課題。
  - **サブエージェント/オフロード全文の裏取り強化**（現状は読めなければ降格）。
  - **[2026-07-08 実測・未対処] 事象M＝phantom-attribution（幻の同意帰属）が Tier D の語彙穴**: ユーザー未提起の話題を「**あなたの違和感は的を射ていて**」と幻帰属して長広舌（`f8ec73a7` L29・台帳M＝入力側捏造 H①と同根）。D1（鉤括弧引用）/D3（`その通り/申し訳` 応答マーカー）のどの語彙にも当たらず全 tier 走査 active 0件。**検知案**: 「あなたの〜（違和感/懸念/指摘/疑問/認識）は的を射て／仰る通り／その懸念は正しく」等の同意帰属マーカー＋その対象語が実ユーザー入力に不在、で D3 を拡張。**要 156走査での偽陽性確認**＝実在の指摘への正当同意（対象が実入力に在る）との分離が精度の壁。
  - **[2026-07-08 実測・未対処] メタ議論免罪（増補4 `_tier_b_reference`）が「検知器の実装用語」文脈で漏れる**: 本検知器の仕様を説明する応答（「〈編集した/テスト通過/掃除した〉の完了主張が…」）の「テスト通過」を Tier B `unverified_test_claim` が誤検知し **Stop ゲートが実際にブロックした**（当セッションで実発生＝偽陽性）。真因＝`_tier_b_reference` の近傍メタ密度が `META_DISCUSSION_RE`（捏造/幻覚/検知/台帳…）依存で、「完了主張/カテゴリ/発火/tool_use/降格/tier」等の**実装用語**は拾えず密度1<2で免罪不成立。**対処案**: `META_DISCUSSION_RE` に検知器の実装用語を加えるか、コードブロック/箇条書き列挙（`〈A/B/C〉`）の引用体裁判定を強化。増補4の限界の実例＝検知器を扱う会話は Tier B/E の恒常的偽陽性源。
  - ~~**Stop アダプタ（stop_guard_fabrication.py）の陽性コントロールテストが無い**~~ → **解消済み（2026-07-06・`fix/handover-singles`）**: `.claude/hooks/test_stop_guard_fabrication.py` を新設（`subprocess` でフックを実起動する 5 ケース＝A3 block／Tier B block／clean 素通し／stop_hook_active 短絡／transcript 不在 fail-open。全経路 exit 0・判定は stdout JSON）。**Tier B block の決定化**＝フックが `sentinel_dir` を実 `.claude/` に固定算出するため、フィクスチャ主張 ts を遠未来化して実 `.kotlin_tests_passed` mtime による fresh 減衰（conf 0.8→0.6）を無効化し block を安定再現。5/5 緑・既存エンジン36件も回帰緑。（2026-07-07: Tier C×2＋A2 の陽性コントロール3件を追加＝8ケース）
  - **事象E（対話文脈の捏造）は静的では原理的に不能 → agy 意味監査で運用**: 2026-07-07 ブラインド実証で agy flash が正解非開示のまま事象E の L99 をピンポイント特定（誤検出ゼロ）。定型プロンプトと手順は `docs/reference/hallucination-ground-truth.md` 冒頭の追記手順 3. に統合済み。ライブゲート化（Stop への組込み）はレイテンシ的に不向きのため**事後監査**運用。
  - ~~**入力側捏造（事象H・I）＝幻のユーザー発話への応答が Tier A/B/C の構造的盲点**~~ → **解消済み（2026-07-07・`meta/hallucination-classifier-v3`＝Tier D 新設）**: アシスタント発話中の〈ユーザー発話への言及・引用〉を実在入力（user human 入力／queued_command(origin.kind=human)／AskUserQuestion 回答）と突合する3ルール（D1 幻引用／D2 幻報告の数値突合／D3 幻応答＋thinking sig 異常ブースト）。正解データ H 2/3（L43 は突合不能で設計上対象外）・I 1/1 検知、slug 全128ファイル走査で偽陽性ゼロ、**新規検知1件（177f88f3 L195＝人間レビューで確定・台帳 J 事象として登録済み）**。軸2（thinking 異常）は較正実測により単独ルール化せず D3 の昇格条件に限定。Stop フックへの昇格は CLI 運用実績を見てから（未実施）。詳細は ADR 0006 増補2。
  - ~~**Tier D の K型盲点（2026-07-08 台帳K）**~~ → **解消済み（2026-07-08・`meta/detector-improve`＝Tier D4 新設）**: 暴走thinking前兆を伴わない入力側捏造（応答継続中に幻のユーザーターンをロールマーカー込みで生成）を、`phantom_turn_marker` ルール＝assistant text 内の `[Request interrupted by user]`（＋"for tool use" 変種）出現で直接検知（K の L320 を捕捉・active conf=0.9）。**軸2（thinking 異常）を昇格条件にしない**のが D3 との本質差（K は sig=1,240＝通常域）＝マーカーリテラルの特異性を根拠に単独昇格。降格は「生成でなく言及・引用」の文脈のみ（`_d4_is_reference`＝メタ語彙／inline・fenced code／「マーカー/marker/割込」名指し／クロスセッション）。**Stop フックへ D4 のみ即時昇格**（`tiers="ABCD"`・blockers に `phantom_turn_marker`(conf≥0.8) 追加。D1〜D3 は非ブロック維持）。slug 全155ファイル走査で active は K の1件のみ＝偽陽性ゼロ実証。詳細は ADR 0006 増補3。**残る限界**: K の謝罪発話 L328（幻応答側）は引き続き `no_thinking_anomaly` 降格（L320 の D4 検知で live block できるので実害なし）。
  - ~~**Tier B のメタ議論免罪欠落（2026-07-08 実測）**~~ → **解消済み（2026-07-08・`meta/detector-improve`）**: 捏造を検証・報告する発話が捏造文言（「回帰テスト：全通過」等）を引用すると `unverified_test_claim` が偽陽性発火していた（K/L検証セッション d2096baa で active 3件）。D4 の `_d4_is_reference` を移植（`_tier_b_reference`）。**Tier B 固有の調整**: 発話全体のメタ語彙密度では真陽性（事象L本体 b4087931＝検知器開発セッション内・whole meta=6）と偽陽性（d2096baa＝whole meta 14〜20）を分離できないため、**whole-utterance でなく〈主張文近傍のメタ語彙密度(±120字で≥2)∨fenced code 内∨成功語トークンの「」引用〉**で降格する（実測ギャップ: 真=near0／偽=near3〜5）。全156ファイル走査で Tier B active 5→2（d2096baa の3偽陽性が降格・真陽性 b4087931 と 441b9875 は active 維持）・他Tier不変。詳細は ADR 0006 増補4。
  - ~~**Stop ゲート検査窓の穴（2026-07-08 台帳L）**~~ → **解消済み（2026-07-08・`meta/detector-improve`）**: 捏造報告の直後に AskUserQuestion 等でターンが継続すると、Stop 検査窓（scope=last_turn＝最終発話のみ）に捏造発話が入らず素通りしていた。**新スコープ `current_turn`＝最後の生ユーザープロンプト以降の全 assistant 発話**を検査（`_last_user_turn_order`。**AskUserQuestion 回答は tool_result で届く＝ターン境界にしない**のが要＝これを境界にすると捏造がまた窓から抜ける）。`stop_guard_fabrication.py` を scope=current_turn 化。全156ファイル走査で live blocker が 2→7 に増え、**新規5件は全て真陽性**（事象L b4087931「回帰テスト：全通過」＋事象F c4b78e7d のマージ確定捏造4件）＝過検知ゼロ。生プロンプト皆無時は安全側で last_turn へフォールバック。詳細は ADR 0006 増補4。
  - **意味照合系検知器（スコープ外構想・旧称「D系」）＝生成コード不具合・外部リサーチ捏造**が必要（正解データ事象B/C）。着想段階。※旧称「D系」のうち帰属誤り（事象A系）は 2026-07-07 の Tier D（入力側捏造）静的検知へ部分昇格し、"D系" の呼称は Tier 文字との衝突を避けて廃止（ADR 0006 増補2「呼称の整理」）。"Tier C" の名も 2026-07-07 に misread 型静的検知へ割り当て直し済み（同増補1）。
- **[kotlin/掃除] 実機の本棚にテスト用シード本2冊が残存**（2026-07-03 Task9 目視関門で `PdfPipelineDeviceTest` が投入）: `spike-N1453LW`/`spike-N2959KI`（+空 `spike-N6169DZ` dir）。掃除の可否をユーザーに確認中で未実施。掃除するなら `filesDir/novels/spike-*` と books テーブルの該当行のみ削除（手動追加のルビ本・他蔵書には触れない）。Phase 3 の実書取込で上書きされる想定でもある。
- ~~**[hooks/fix] マージ統合で露呈したコミットゲートの2つの穴**~~ → **両方解消済み（2026-07-06・大規模マージ後の stale-check フル点検と同時に実施）**。①は `--name-status` 化して D（削除）をゲート対象から除外（Python ゲート自体も Phase 5 追従で撤去）。②は `COMMIT_GENERATING_RE`（merge/rebase/cherry-pick 検知・--abort/--quit/--no-commit 除外）を guard/consume/granularity へ追加し `test_hooks.py` で回帰固定（ADR 0004 追記済み）。**残る既知の限界**: `git pull` と PowerShell 経由は従来どおり対象外。原文は下記に保全:
  - **① `check_commit_granularity.py` の Python/Kotlin ゲートが「削除」を「要テスト」と誤判定**（＝false-positive）。根本原因: ステージ一覧を `git diff --cached --name-only`（削除も列挙される）から取り、`python_staged`/`kotlin_staged` を**拡張子とパスだけ**で絞り、センチネル存在チェック（`if not os.path.exists(SENTINEL): exit(2)`）を**削除ファイルにも適用**している。stale チェック側は `os.path.exists(repo_root/f)` で削除を除外できているのに、存在チェック側だけ削除を区別しない非対称が原因。実害: kotlin マージは `src/main/python/*.py` を**全撤去**するため、`.python_tests_passed` センチネルが偶然残っていなければ「`python -m unittest test_logic` を実行せよ」と**削除済みで実行不能なファイルのテストを要求してコミット不能**になっていた（今回は既存センチネルが在り通過）。修正案: `--name-status` で拾い `D`（削除）ステータスを `python_staged`/`kotlin_staged` から除外する（追加・変更のみゲート対象＝削除にテストは不要）。
  - **② コミット系ガードが `git merge --continue` / 競合なしの `git merge`・`rebase`・`cherry-pick` を素通しする**。根本原因: `guard_commit_branch.py` と `check_commit_granularity.py` はどちらもコマンド文字列を**リテラル `git … commit` の正規表現でしか検知しない**（`COMMIT_CMD_RE` / `\bgit\s+commit\b`）。マージ完了・rebase 継続は "commit" トークンを含まず**コミットを生成する**ため、両ガードを通過する。今回は競合したため明示 `git commit` が必要になり両ガードが発火したが、**競合しない main 直マージは branch guard もテストゲートも素通り**する。CLAUDE.md も branch guard を「ソフトな防御網」と明記済み＝既知の限界だが、マージ完了経路が盲点である点を追記。修正案: 両フックの検知に `git\s+(merge|rebase|cherry-pick)\b.*--(continue|no-ff)` 等のコミット生成コマンドを加えるか、PreToolUse でなく実際のブランチ HEAD 移動を捕える PostToolUse 併用を検討。
  - 補足（運用ノウハウ・2026-07-06 更新＝人間発行方式へ移行）: **センチネルは AI が作成できなくなった**（新フック `guard_sentinel_creation.py` が Write/Edit/Bash 経由の `.allow_protected_commit` 生成を PreToolUse でブロック）。main への明示コミットは〈AI がコミット内容を提示 → **ユーザーが入力欄で `! echo > .claude/.allow_protected_commit` を実行**（先頭 `!`＝bash mode だけがフックを迂回する＝2026-07-06 実測確定）→ AI がコミット〉の流れ。旧補足〔AI が `echo > … && git commit` を同一 Bash に混ぜると検査時点でセンチネル未作成のため失敗した〕は、AI が作成不能になり人間の `!` 実行と AI のコミットが自然に別呼び出しへ分かれるため解消。**限界**は難読化・PowerShell 経由が対象外（ADR 0004 のソフト境界）。
  - **[docs/宿題・main 統合後] memory 整合**: 本機構が main へ統合されたら auto-memory `git-c-commit-guard-false-block`（「Bash echo は通る」）を「Bash echo も guard_sentinel_creation でブロック・人間の `!` のみ発行可」へ更新する。**なぜ統合後か**: auto-memory はパス紐付けで全ブランチ共有＝機構が main の現実になる前に書くとブランチ境界を破る。

---

## A. 変換まわりの機能要望（**全件完了**・2026-06-23 lab検証中にユーザー発案）

> ①③④は **2026-06-25 実装完了**（実機目視OK）。①の割り込み停止（ページ境界の即中断への再配線）と
> ② 強制終了時の通知/再開（Room v8 `pending_jobs`・起動時リカバリ・孤立HTML掃除・`takePersistableUriPermission`）は
> **2026-07-07 実装＋実機検証完了**（`feat/processing-resilience`）。詳細・検証エビデンスは `STATUS.md`、
> 実装のアーキテクチャは `.claude/skills/architecture` Service層の項が正本。本節に残タスクなし。

## A2. UI-n ブランチ（見た目の白紙改装・2026-06-26 フェーズ0完了）

- **フェーズ0完了＝D「和モダン・余白」をデフォルト視覚言語に採用。** 本棚案A〜Jの10案を作り横並び選定した結果。なぜDか等の恒久設計判断は `docs/decisions/0005-ui-n-visual-language-D.md` が正本、モック地図（A〜J署名要素）・与件・ワークフローは `.claude/plans/UI-n_DESIGN_PLAN-archived-2026-07-02.md`（§6.1）に保全。
- **方針確定（2026-06-27・ユーザー指示）＝UIスキン着せ替え（A〜J 選択）はまだ実装しない。main は現状 D のみ。** A〜J は資産として claude.ai/design に保持するが、スキン切替機能は将来送り。
- **第2バッチ（2026-06-27 実施済み）**: ① D で読書・目次・読書設定をHTMLモック化＝**完了**（`ui-n-phase0/reading-D.html`・`toc-D.html`・`settings-D.html` を push）。読書は3テーマ（ライト/セピア/ダーク）＋クローム表示/没入の見た目を併記、目次は現在章=左藍ルール＋淡背景＋明朝太字＋空状態、設定シートはテーマ3択（藍選択）/文字サイズ/行間。
- **第3バッチ・D実機ループ（2026-06-30〜07-02・④⑤⑥完了で主要一段落）**: 実機スクショ↔Dモック突合でCompose翻訳を仕上げ。
  - 完了: `Color.kt`(`cb09392`)/`Theme.kt`(`c6da6cf`)/`BookCover.kt`(`20dcc00`)/読書hr(`cd4853f`) ＋ **① 本棚 D完全準拠**(`461cf7c`：フラット編集・明朝題字・書影下部タイトル・藍進捗/青磁未読・⋮メニュー化) ＋ **③ 読書 章見出し明朝＋藍ルール・前書き後書きラベル藍**(`35eae10`) ＋ **テーマ単一正本同期**(`e93d2eb`：本棚⋮/設定シートどちらでも切替→全体追従。セピアは本棚ライト流用)。
  - **④⑤⑥ 完了（2026-07-02・実機ダーク目視OK）**: ④ 明朝トークン統一(`e791e97`：`FontFamily.Serif`直書き14箇所→`MinchoFamily`) ／ ⑤ 目次 toc-D(`f708739`：題字明朝＋字間.12em・現在章淡背景0.06) ／ ⑥ 設定シート settings-D(`1bfb4a9`：見出し明朝＋字間.08em)。**D実機ループの主要残件はこれで解消。**
  - ~~**残る微調整（任意・後日）**~~ → **全て解消済み（2026-07-02 単発修正バッチ・コミット表=STATUS）**: スライダー目盛り消し（※「カスタム track が要る」は誤りで tickColor 透明化で足りた＝`task_diary` #29）／現在値の右寄せ藍数字化／AutoMirrored 警告解消。
  - 手順書＝`.claude/plans/ui-n-D-completion-loop-HANDOVER-2026-06-30.md`（**必ず UI-n 上で起動**＝ブランチ跨ぎ hook 破綻回避）。色は `Color.kt`/`Theme.kt` 経由＝直書き禁止／字面は `Typography.kt`。
  - 環境知見: WSLビルドを実機へ上書きinstallするには Windows debug.keystore の共有が要る → memory [[wsl-debug-keystore-share-for-install]]。
- **将来送り（保留・元第2バッチ②③）**: 「UI着せ替え」設定画面のモック化（選択肢=A〜J・既定=D・切替粒度の決定）／A〜J スキンの Compose 実装（スキン×読書テーマの関係・トークン体系）。スキン機能に着手する時はここから再開する。
- モック正本は claude.ai/design プロジェクト `Novel Reader UI`（projectId `bb5a35c8-70ac-4efa-bb03-1579d3f11d93`）の `ui-n-phase0/` 配下。`DesignSync: get_file` で再取得可。

## B. 本棚

- ~~**案B 詰め直しアニメ復活**~~ → **解消済み（2026-07-07・`bcb5216`(BOM 2025.02.00＝1.7系最終)＋`144490b`(animateItem置換)）**: Foundation 1.7系で stable 化した `Modifier.animateItem()` をグリッド/リストの2箇所へ適用し、削除時の詰め直しアニメを復活。受入=①詰め直しアニメ動作 ②高速フリング重なり非再発（案Aで消した1.6系バグ） ③全画面回帰（本棚list/grid・読書・目次・設定シート・テーマメニュー）を実機目視OK。副次の見た目差分: material3 1.2.1→1.3.1 で設定シートのスライダーが新標準意匠（太トラック＋バー型つまみ）に変化＝機能正常・意匠を旧様式/モック準拠へ寄せるかは別途判断（thumb/track スロットAPIで変更可能）。一次情報=`.claude/plans/bookshelf-reflow-anim-HANDOVER-archived-2026-07-07.md`。
- ~~**11 本棚テーマ追従**（見送り）~~ → **解消済み（2026-07-01・`e93d2eb`）**: テーマ正本を `MainActivity` へ巻き上げ、本棚(`NovelReaderTheme(darkTheme=theme==DARK)`)も読書も単一の `ReadingTheme` 正本に追従。本棚⋮メニュー/読書設定シートのどちらで変えても全体同期。~~セピアは本棚ライト流用（専用セピア本棚は将来拡張の余地）~~ → **セピア追従も解消済み（2026-07-07・`fa24366`）**: ライト流用が「ライトとセピアの差がない」フィードバックの主因と判明し、`SepiaColorScheme` で本棚・発見系も琥珀紙へ追従。

## C. UI見送りサブ（2026-06-25 スコープ外決定）

- ~~**05 本文左右余白/行長**・**06 本文余白の設定化**~~ → **解消済み（2026-07-02・`c5959ae`）**: 統合実装。`reading_body_margin` prefs（10〜40dp・既定15）＋設定シート3本目スライダー＋広幅端末の中央寄せ。
- ~~**09 グリフ太さ/紙質感/色味の追微調整**~~ → **打ち切り（現状維持・2026-07-02）**: 再開点メモの起点値（`saturation 0.38〜0.49`/`lightnessTop 0.46〜0.61`）は**旧HSL書影の値で陳腐化**していた。書影は既にD様式の暗色スラブ（彩度12〜21%/明度上26〜34%）へ刷新・調律済み（`20dcc00`→`9f00680`→`ede761f`＋パレット調律`6e16072`）。実機ベースライン（リスト/グリッド）確認の上ユーザー判断で現状維持を選択。以後、書影の見た目はモックD（`ui-n-phase0/bookshelf-D.html`）＋ADR `docs/decisions/0005-ui-n-visual-language-D.md` が正本で、Compose 側の自己判断色味調整は行わない（CLAUDE.md「UIの見た目は /design モックが正本」ルール準拠）。プラン一次情報＝`.claude/plans/single-fix-batch-archived-2026-07-02.md`。

## D. 長期・品質（旧handoverから保全）

- **Chaquopy→Kotlin(PDFBox)ネイティブ化【Phase 1〜5 全完了・2026-07-05／現況は STATUS.md 参照】**: A/B評価で **B案が技術的に優位**と判定済（`ab-review/submission-B` に完成形プロトを残置＝精度オラクルとして今後も温存）。知見 = `[[kotlin-pdfbox-migration-prototype]]` / 回帰基盤 = `[[golden-regression-baseline]]`。Phase 1 移植＋垂直スライス・Phase 3 配線＋実機検証・Phase 4 精度回帰ゲート・**Phase 5 Chaquopy/Python 完全撤去（APK 67→24MiB・64%減）**まで到達し、ランタイム抽出は純 Kotlin(PDFBox) 単独。**次段は `kotlin`→`main` 統合**（統合時に auto-memory・global `~/.claude/CLAUDE.md` の Python テスト記述も追従更新＝下記）。純 Kotlin 化により `Dispatchers.IO` での真の並列処理が可能な土台になった（複数冊同時処理の実装は将来送り）。残る抽出エッジは下記。
  - **[残タスク] 超長編PDFの抽出エッジ残差**（2026-07-03 検出・2026-07-05 Phase 4 で全容判明・優先度低）: N6169DZ(116650段/350万字)で pdfminer 比 **文字+434(+0.012%)・ルビ+110(+0.97%)・段落+5・blank+1・章題グリフ写像差 11件**の残差。短中編(N1453LW/N2959KI)は body_sha256 完全一致なので**超長編固有の抽出エッジ**（座標順・グリフ写像＝pdfminer が吸収していたケース）。
    - **章題11件の内訳（2026-07-05 Phase 4 で判明。旧記載「1件」は旧spikeが最初の差だけ表示した過少記録）**: ①**ダッシュ変種 `－`(U+FF0D)→`−`(U+2212) が6件** ②**矢印回転 `↑↓`(U+2191/2193)→`←→`(U+2190/2192) が3件**（PDFBox が矢印を90°回転誤読・golden の←→が意味的に自然） ③**アポストロフィ座標順 `兎'ｓ`↔`'鳥…` が2件**。
    - ~~**[改善候補] ①②の9件は1:1コードポイント置換で golden に寄る**~~ → **解消済み（2026-07-06・`fix/handover-singles`・実機ゲート合格）**: 波ダッシュ(FF5E→301C)と同様 `PdfExtractor.normalizeGlyphUnicode` へ `FF0D→2212`・`2191→2190`・`2193→2192` を個別 indexOf ガードのチェーンで追加（`assertSame` 契約維持・`PdfExtractorTest` に写像/非対象テスト追加・`testDebugUnitTest` 緑）。**実機ゲート合格（`am instrument PdfExtractorDeviceSpikeTest`・PGEM10）**: ①短中編 N1453LW/N2959KI の body_sha256 完全一致を維持（`PdfAccuracyGate` 実測 `057159caa954…`/`8c697ad02d25…`＝golden 一致・章題不一致0件＝**`FF0D→2212` は本文を壊さず**）②N6169DZ 章題ドリフトが **11→2件**へ減少（残2件＝1:1置換不可のアポストロフィ座標順・想定通り）。縮退分岐(`FF0D→2212` 取り下げ)は不要だった。③のアポストロフィ座標順は 1:1置換不可（別途・対象外）。
    - 基準=`ab-review/golden_regression`、再現=`PdfExtractorDeviceSpikeTest`（Phase 4 で精度回帰ゲート化）、詳細=task_diary #35。※波ダッシュ(U+FF5E↔U+301C)は抽出時正規化(FF5E→301C)で対処済み。
  - ~~**[Phase 4 詰め] 精度回帰ゲートの ≤15 版クリーン合格ランを取り切る**~~ → **完了（2026-07-05）**: 実機で N6169DZ 含む3件がゲート通過＝`OK (1 test)`（Tests run:1 Failures:0）取得。合格ライン=全PDF title/author/章数=完全一致・短中編 body_sha256=完全一致・長編は数値許容帯（char±0.05%/ruby±1.5%/para±8/blank±8＋章題≤15件不一致）。**ハマりどころ(task_diary #38)**: ハングの真因は ColorOS の Hans フリーザ(OplusHansManager)が素の androidTest を **freeze** することで、#37 の fg_cpu **kill** とは別機構（端末操作/充電の有無に無関係）。**ゲートを再実行する場合の作法**: `am force-stop` で競合削減＋`am instrument -w -e class com.novelreader.pdf.PdfExtractorDeviceSpikeTest com.novelreader.test/androidx.test.runner.AndroidJUnitRunner` を回し、**実行中に `adb shell monkey -p com.novelreader -c android.intent.category.LAUNCHER 1` で MainActivity を前面化**してプロセスを perceptible に保つ（前面化で %CPU 0→250% へ復帰し完走）。資産は `androidTest/assets/spike/`（gitignore・`sample_pdfs/`＋`golden_regression/` から配置）。現況は STATUS.md 参照。
  - ~~**[残タスク][UX] 本文抽出中に進捗バーが実時間連動しない**~~ → **解消済み（2026-07-06・`fix/handover-singles`・実装＋実機フィードバック反映）／統合表示の最終目視のみ次回接続で再確認**（2026-07-05 Phase 3 実機検証で N6169DZ 取込中にユーザー観察）: 「本文を抽出しています…(ステップ2/4)」の間、下部バーが本文0%付近で止まって見え、抽出完了後に一気に進む問題。**原因**: `PdfExtractor.runFinalEngine` は `loadPages(doc)`＝全ページのグリフ抽出（PDFBox `PDFTextStripper.getText` の単一オペ＝超長編の支配的コスト）に**進捗コールバックが無く**、その後の `TextProcessor.processPages` だけが per-page 進捗を出すため。**対処**: `GlyphStripper.startPage` にページ境界フック（`onPageStart`）を追加→`loadPages` に `onPageLoaded` を通し、`runFinalEngine`/`PdfHandle.runEngine`/`PdfBoxEngine` を `EnginePhase{LOAD,PROCESS}` 付き通知へ変更。facade(`PdfBookExtractor`)が `LOAD_WEIGHT=0.75` で load(重み大)→process(重み小)を重み合成し、step-1 local を 0→0.75(load)→1.0(process) と単調前進させる。`PdfBookExtractorTest` を厳密アサートへ更新（`testDebugUnitTest` 緑）。`ProcessingBanner` は変更不要（step-local をそのまま描画）。**実機で load 進捗の前進を客観捕捉**（実書6,551ページ取込で通知の n=76→6,551 が単調増加・process へも継続）。ただし当初の「読み込み/抽出」を別ラベル・別カウンタで見せる版は、カウンタが 0→N を2度満ちて『一度終わって2周目に入った』ように誤認される問題をユーザーが実機で発見（表示は各フェーズでリセットするのにバーは両者を1本で 0→100% にするため食い違う）。**対処（実機フィードバック採用）＝本文ステップを単一フェーズ「本文を処理しています… X%」へ統合**（`grouped`/ページ数表記を撤去し、バー(step-local)と一致する通し% を表示・カウンタのリセット無し＝「2周目」錯覚を解消）。JVM 緑。**統合表示の実機目視は次回接続で再確認**（`8277c91` 以降のコミットで実装）。
  - ~~**[予約] Phase 5（Chaquopy/Python 撤去）完了時に併せて更新するもの**~~ → **完了（2026-07-05・Phase 5 と同ターン）**: ①`.claude/skills/stale-check/check_machine.py` の `check_referenced_files()` の Python 必須ファイル hardcode を Kotlin `pdf/` へ差替・`check_versions()`/`check_test_commands()` の Chaquopy/Python 照合を撤去（撤去後 stale-check は high 0/info 0 のクリーンを確認済） ②CLAUDE.md 冒頭のビルド設定宣言を「PDFBox-Android 2.0.27.0」へ・自己検証ルールとドメイン知識の Python 参照を Kotlin へ ③build スキルの「Python PDFロジックの単体テスト」節を撤去し testDebugUnitTest へ集約 ④architecture スキルのパイプライン図を `PdfBookExtractor` 単独経路へ全面書換（二重構造記述を解消）。~~**残（main 統合時に実施）**: auto-memory の build 記述・global `~/.claude/CLAUDE.md` の Python 単体テスト節の追従更新~~ → **消化済み（2026-07-06・main 統合後の stale-check フルで実施）**: global `~/.claude/CLAUDE.md` の Python 単体テスト節を撤去し testDebugUnitTest へ統合・py シム行を歴史注記化。auto-memory は grep 確認で Python テスト手順の記述なし（pdfminer 言及は歴史的知見のみ＝維持）。
- **左右スワイプで章遷移**: 旧 `experiment`/`lab-old` は WebView 実装で流用不可。`HorizontalPager`/`pointerInput` で新規。チューニング知見＝軸ロック(`de60869`)/EMA+isDragging(`a07dd3e`)/距離OR速度複合(`4a0719b`)。元コミット `23b5f33`(main未取り込み)。
- **BookRepository インターフェース化**（テスト可能化）: 具象直参照＋static シングルトン(PDFBoxResourceLoader の Application 初期化/Room)で JVM単体テスト不可。interface 抽出＋`FakeBookRepository`。影響 `BookRepository.kt`/`NovelReaderApplication.kt`。
- **Phase3 外部連携**: ①内部ブラウザからPDF直接取込＆動線追加 ②「小説家になろう」公式API連携・ランキング表示（`docs/reference/narou_api_manual.md` 参照）。
- ~~**doc アーキの main↔lab 乖離**（2026-07-02 発覚）~~ → **解消済み（2026-07-02）**: lab の実質新規（変換 A①③④・DB v7・想起フック修正・実装知見）を main へ論理単位で統合し、doc 正本を main へ一本化・lab を廃止した。規約と実態の逆転（旧「整頓は lab で」）は解消し、以後 doc 整理は main で行う。※main 側の綻び（STATUS.md デッドリンク・ADR 0001 二重採番）も 2026-07-02 に解消済み。
