# 正本モック陳腐化の棚卸し（コード→モック逆同期リスト）— 2026-07-16

- 発端: ユーザー指摘「改善案が過去のモックをもとに作られている」（同日、複数選択削除モックが旧世代 bookshelf-D 基盤で生成された事故で顕在化）。
- 実施: 読み取り専用棚卸しエージェント。正本の定義は ADR 0005（主要4画面＋発見系6画面）。
- 前提の時系列事実: 2026-07-13 `a1653e6` は正本13枚の**余白7段離散化のみ**＝07-13 の日付は内容同期を意味しない。
- 対処済み（同日）: プレビューの「古い版が出る」事故クラスは `mockview`（スナップショット方式・グローバルCLAUDE.md 記載）で構造的に解消。

## 棚卸し表（要点）

| 正本モック | 対応画面 | 未反映の視覚変更 | 深刻度 |
|---|---|---|---|
| discovery/bookshelf-fusion-D | BookshelfScreen/ShioriCover | ①発見帯 collapse「完全退避」＋フィルタ sticky（`c10679e`）②栞ランダム先端/棒長（`f208608`）③取込バナーは**要素自体が未カバー** | **高** |
| bookshelf-D | （fusion 前の旧骨格） | fusion 再設計前の世代＝発見帯・続きあり・なろう未取込を欠く＋栞ランダム未反映 | **高**（提案基盤に使うと退行） |
| discovery-home/genre/search/detail/reading-continuation | discovery 系 | InfoText AA（`a9a6a5c`）未反映＝`--ink-soft` のまま（計 ~49 箇所）。検査は未トラッキングで PASS に化ける | 中 |
| settings-D | ReadingSettingsSheet | スコープ注記（T2/残7⑤）は本日モック・コード両方に追加済み（未コミット）＝ズレなし | 低 |
| reading-D / toc-D | 読書/目次 | ギア下端集約はコミット同時同期済み。残差はモーション＝ADR0005§B で仕様上モック対象外 | 低/なし |
| discovery-detail-D の CTA 階層 | NovelDetailScreen | 残6 改修が未着手のためモック・コード共に旧階層で**一致**＝改修時にセットで改訂 | 低 |

## 逆同期の優先順位（裁定）

1. **fusion-D への注記**（即時・実施済み）: 「退避構造（07-14）未反映・残1（スクロール連動方式への再設計 deferred）確定待ち」＝方式が動きうるため全面描き直しは残1確定後。
2. **bookshelf-D の退役注記**（即時・実施済み）: fusion-D に統合済みの旧世代＝以後の提案基盤に使わない。
3. **複数選択削除モックの再基盤化**（即時）: fusion-D 骨格へ作り直し。
4. 栞ランダム抽選の fusion-D 反映・取込バナーの fusion-D 追加＝中期（handover へ）。
5. InfoText AA の逆同期＝既存の留置タスク（handover「UI/UX宿題」）のまま＝`--info-ink` 新設＋検査マッピング追加とセット。

## 恒久ルール（再発防止）

- モックのプレビューは必ず `mockview`（素の `chrome <file>` 禁止＝古いコピー/キャッシュ事故）。
- 実機後詰め層（§B）や監査消化で**コード先行の視覚変更を入れたら、正本モックへの逆同期 or 「未反映」注記をセットで行う**（注記が無い正本は「最新」と信じられてしまう）。
- **裁定が済んで台帳/ADR から参照が消えるモックは、参照を消すのと同じコミットで削除する**（git 履歴が保管庫。無印の残骸モックは「提案基盤に拾われて退行」の温床＝2026-07-16 に探索期の孤児6枚＋准孤児1枚を削除済み）。
