# 実機検証6件（UX監査 残2）結果 — 2026-07-16

- 端末: OPPO PGEM10 / com.novelreader v19 debug（ブランチ ui/polish）・実蔵書8冊で実施・全件原状復帰済み
- 実施: 自律検証エージェント（read-only・破壊操作なし）。スクショ等の生証拠はセッション scratchpad（揮発）＝本ファイルが恒久一次情報。

## 結果サマリ

| # | 項目 | 判定 | 要点 |
|---|---|---|---|
| 1 | 回転レース | **PASS** | フリング直後（debounce 400ms内）即回転×3サイクルで位置喪失なし。機序: ON_STOP フラッシュ（DisposableEffect→CONFLATED→config 変更で生存する ViewModel の IO 書込）が再生成側 getProgress の DB 読取に構造的に先行するため、レースに勝つ。回転は Activity 完全再生成（configChanges 未宣言）。 |
| 2 | 回転オフセット | **PASS（軽微）** | 段落 item index は保持。段落内 px offset（例 408px）は reflow で無効化され段落先頭へスナップ＝約1行の後退ドリフト。**常に現段落内に有界**（段落を跨ぐ位置ロストは無い）。 |
| 3 | ノッチ横向き | **PASS** | ROTATION_90/270 とも本文はカットアウトインセット（160px）の外。行頭・行末の欠けなし。 |
| 4 | TalkBack到達 | **FAIL** | クローム非表示時、戻る/目次/前後章/表示設定が a11y ツリーから**完全消失**（clickable ノード0・本文段落は残存）。機序: バーは graphicsLayer{translationY} で画面外退避→Compose が画面外ノードを a11y ツリーから除外。TalkBack スワイプ走査で没入状態から到達不能（上スクロールでのクローム復帰のみ可能）。検証手法: TalkBack は有効化せず uiautomator dump（同じ AccessibilityNodeInfo ツリー）で静的確認。 |
| 5 | ルビ掛け | **PASS** | 長ルビ「ドッグズ・チェーン」(9字・基底の約2倍幅) は隣接非ルビ文字上へ肩付きはみ出しで衝突なし。隣接単字ルビ 魔[マ]力[ナ] も明確に分離。※「長ルビ×ルビ隣接」の組合せは実データに不在＝未観測（残る未知はこの組合せのみ・低優先）。 |
| 6 | 大フォント行数 | **PASS** | 24sp+余白40dp≒11字/行・18sp+余白10dp≒18字/行＝事前予測と一致（予測は余白依存で成立）。最大設定でもルビ・本文の欠け/オーバーフローなし。 |

## 派生タスク

- **#4 の是正**（要改修・handover へ登録）: 没入時もナビへの a11y 到達を残す。是正例＝translationY 退避を a11y から消えない形にする（alpha 退避＋semantics 維持等）。**意匠・没入クロームの挙動絡み＝/visual-language ゲート経由で方式判断**。
- #2 の段落内1行ドリフトは有界かつ体感軽微＝現状受容（不快の報告が出たら再訪）。

## 記録事項（無害・透明性）

- 検証1のスクロール操作で蔵書 768c0eb5 の読書位置が chap_1 index 738→669 へ移動（章末近傍同士・読了印 reachedEnd=1 は保持）。通常の読書操作由来で無害。
