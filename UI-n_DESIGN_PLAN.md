# UI-n ブランチ — 見た目の白紙設計（Design主導 UI全面改装）計画

> **この md 一枚で、別セッションの Claude Code が UI 作業へ単独で入れることを目標にした作業指示書。**
> `UI-n` は **既存UIの見た目を思想設計の段階から白紙で作り直す**ための破壊可能ブランチ。
> 既存の配色・トークン・書影ロジック・タイポは **すべて捨てる**前提。`main`/`lab` への影響を気にせず破壊的に試してよい。
> 見た目（静的視覚）の正本を **Claude Design（HTML）** で立て、それを Compose 実装へ翻訳するワークフローを回す。

---

## 0. 役割分担（最重要・認識合わせ）

| 主体 | できること | できないこと |
|---|---|---|
| **Claude Design**（`DesignSync` ツール + `/design-sync`） | claude.ai/design 上の **HTML デザインシステム**の読み書き＝見た目の正本／モックを作る | **Compose コードは一切書き換えない** |
| **Claude Code**（このセッション種別） | HTML で決めた値・レイアウトを **Compose（Kotlin）へ翻訳**して実装 | デザインの良し悪しの最終判断（人間が目視で決める） |

→ **「Design に丸投げで全UIが自動で変わる」は仕組み上あり得ない。** HTML と Compose は別言語の別ファイルで、両者を繋ぐコンパイラは無い。この md は両者の橋渡し。
HTMLカタログ＝見た目の決定 → Claude Code が翻訳 → 実機で確認、の往復が基本サイクル。

## 0.5. このブランチの方針（前版からの転換・最重要）

**前版は「既存UIの保守的チューニング」指示書だった。本版は「見た目の全面白紙設計」指示書である。** 混同しないこと。

- **白紙にする対象**: 配色哲学・タイポ階層・余白/構図・コンポーネントの設計言語・画面構造のレイアウト。
- **白紙にしない/できない対象**: 横書きルビ組版（ふりがな上付き。`RubyText.kt`／`RubyLayoutHelper.kt` 系。AnnotatedString＋Canvas重ね描きで禁則処理込みの組版エンジン＝移植リスク大ゆえ対象外）、PDF処理パイプライン・DB・Service 層。これらは機能であって見た目ではない。
- **既存の視覚資産は「踏襲すべき正本」ではない。** 既存の `ReadingColors` 13トークン、朱墨色 accent、HSL生成書影、現行タイポは **すべて破棄候補**。§2 で既存ファイルを Read するのは *現状把握* のためであり、*踏襲のためではない*。
- **スコープは見た目（静的視覚）のみ。** 操作感・組版の質感・アニメーション・没入クロームの挙動は今回の設計スコープ外。実機フィードバックで後から詰める（理由: フォントレンダリング・行間の見え方・transition のイージング等は HTML→Compose で最も劣化する層であり、ブラウザ ≠ 端末だから）。

## 0.6. Design への渡し方（与件は渡す・美学は指定しない）

「すべて任せる」を**情報ゼロで投げない**こと。情報ゼロだと Design は無難で平均的な一般読書アプリに収束し、縦書き前提の設計が抜ける。

- **与件（必ず渡す＝アプリの機能的事実）**:
  - 日本語の**横書きルビ小説リーダー**（小説家になろう等のWeb小説を読む用途。本文は横書き＝ふりがなは親文字の上に付く）
  - 主目的は**長時間読書と没入**
  - 主要画面: 本棚 / 読書 / 目次 / 読書設定
  - ライト/セピア/ダークの**3読書テーマ**が要る（配色そのものは白紙でよいが、3モード構成は与件）
  - 本文は**横書き＝ふりがな（ルビ）が親文字の上に乗る**前提の領域が要る（実装は別だが、設計はそれを想定すること）
  - **書影に実画像が無い**（bookId から自動生成）。本棚は「表紙画像ありき」で組めない＝生成書影の見せ方ごと設計対象
  - **文字サイズ・行間はユーザー可変**（読書設定にスライダー）。本文レイアウトは可変前提で設計する
  - **フォームファクタ**: スマホ縦持ち・タッチ・片手で長時間読む。タップ標的/親指動線/没入クロームの根拠
  - **コンテンツの形**: なろう系＝章数の多い長編／章タイトルが長い／本文に前書き・後書きブロックとシーン区切り線が出る
- **美学（あえて指定しない＝Design に複数案を出させる）**:
  - 配色の方向性、質感（紙的/デジタル的/文芸的 等）、タイポの個性、構図の哲学。
  - → **Design に複数案（バリアント）を出させ、横並びで現物を見てから人間が選定**する。

## 1. ワークフロー（Design ⇄ Compose の往復）

```
1. claude.ai/design にデザインシステム・プロジェクトを用意
   （DesignSync: list_projects → §6 の projectId を確認。無ければ create_project して §6 に記録）
2. §0.6 の与件を Design に渡し、美学は指定せず「見た目の複数案」を HTML で作らせる
   - 各プレビュー HTML の1行目に  <!-- @dsCard group="…" -->  を入れる（カード索引が自動構築される）
   - 順序は必ず: list/read → finalize_plan（書く/消すパスを確定）→ write_files/delete_files
3. 複数案を横並び比較 → ユーザーが目視で選定
   - 良ければそのまま採用へ。不十分ならフィードバックして再生成（往復）。
4. 採用案の値・構造を Theme.kt / 各 Composable へ「翻訳」して反映（= Claude Code の仕事）
   - 既存トークンに引きずられず、採用案に合わせて再設計してよい（破棄前提）
5. 実機ビルドで最終確認（§5）。フォント差・行間・操作感は実機フィードバックで後詰め
```

**直結の度合い**（期待値の調整）:
- 数値の移送（色・サイズ・余白・角丸）= **高**（1対1で写せる）
- レイアウト構造の移送（flex→Row/Column、grid→LazyVerticalGrid）= **中〜高**
- 最終的な質感（端末フォント・行間・アニメ・操作感）= **低**（実機フィードバックで詰める＝スコープ外）

## 2. 既存実装マップ（現状把握用 ＝ 踏襲ではなく破棄候補）

> 既存の色・サイズの具体値は md に複製しない。下記ファイルが現状の所在。**Design化のとき現状把握のため Read するが、値の踏襲は前提にしない。** すべて作り直し対象。

### テーマ層（現状）
- `ui/theme/Color.kt` — Material3 ライト/ダークの全ロール（現 primary=朱墨色 等）
- `ui/theme/Theme.kt` — 読書3テーマ `ReadingColors`（現 LIGHT/SEPIA/DARK × 13トークン）。Material3 ColorScheme もここ
- `ui/theme/Typography.kt` — 現状の日本語向けタイポ設定

### 画面・コンポーネントの現状レイアウト（白紙設計の土台ではなく、現状理解のため）
- **本棚 `ui/BookshelfScreen.kt`** — TopAppBar「本棚」＋アクション、グリッド/リスト切替、FAB「PDFを追加」、処理中バナー、`GridBookCard`/`ListBookCard`、`EmptyBookshelf`、`ProcessingBanner`
- **書影 `ui/components/BookCover.kt`** — 現状は bookId ハッシュ→HSL色相で縦グラデ生成＋先頭1文字。**このロジックも破棄候補**（白紙設計では書影の見せ方ごと再検討してよい）
- **読書 `ui/NativeReadingScreen.kt`**（公開名 `ReadingScreen`）— `ChapterScreen`（オーバーレイ式トップ/ボトムバー）、`ChapterContent`（LazyColumn・本文幅・段落描き分け）、`ReadingSettingsSheet`
- **目次 `ui/NativeTableOfContentsScreen.kt`** — 「目次」リスト、現在章ハイライト、自動スクロール

### 対象外（白紙設計に含めない＝機能であって見た目ではない）
- 横書きルビ組版（ふりがな上付き） `ui/compose/RubyText.kt` ＋ `ui/compose/RubyLayoutHelper.kt`（AnnotatedString＋Canvas重ね描き。禁則処理込みで移植リスク大）
- 既知不具合#1（ルビ位置ずれ）、PDF処理パイプライン・DB・Service 層

## 3. 進め方（backlog）

### フェーズ0. 視覚言語の白紙設計 ★最初の一手
- [x] §0.6 の与件を Design に渡し、**見た目の複数案**を HTML で作らせる（配色哲学・タイポ階層・余白/構図・コンポーネント設計言語をゼロから）
- [x] 主要画面の方向性が分かる粒度でモック化（**本棚＝A〜Jの10案**まで拡張。読書はA/B/Cのみ）
- [x] 横並び比較 → ユーザーが目視で選定（2026-06-26 完了）

#### フェーズ0 結果（2026-06-26 確定。別セッションはここから再開）
- **採用＝D「和モダン・余白」をデフォルト視覚言語に確定。** 寒色の白×藍(#1C3D5A)のヘアライン×青磁(#9CB3A8)、余白主導の静かなエディトリアル。本文＝明朝／ラベル＝ゴシック。書影＝静かな縦グラデ＋縦の藍ルール（実画像を捏造しない）。
  - **なぜD**: 「ユーザーが長時間落ち着いて読む」用途に最も素直。奇をてらわず可読・静謐で、3読書テーマ（ライト/セピア/ダーク）へ展開しやすい寒色基調。斬新案(H/I/J)は魅力的だが本命の土台はDとした。
- **新与件＝UI着せ替え（カスタマイズ機能）。** D をデフォルトに、**作成した A〜J 全案をユーザーが選べる「UIスキン」として残す**方針が確定。→ 視覚言語は1つに固定せず、スキン切替を前提に設計する（切替の粒度＝アプリ全体スキン or 画面ごとは次セッションで詰める）。
- **保留→全保存**: 最終比較で C・D・F・J に絞ったが、最終的に **A〜J 全案をスキン資産として保持**する判断。
- 各案の現物（HTML）は §6 のプロジェクトに保存済み（ローカル scratchpad はセッション破棄で消えるため、正本は claude.ai/design 側）。

### フェーズ1. 採用案の詰め（フェーズ0で方向が決まった後）
> **第2バッチ（2026-06-27 実施）**:
> ① D の視覚言語で **読書・目次・読書設定** を HTML モック化 ＝**完了**（`ui-n-phase0/reading-D.html`・`toc-D.html`・`settings-D.html`。§6.1 参照）。
> ② ~~「UI着せ替え（スキン選択）」設定画面のモック化~~ ＝**保留（将来送り）**。**ユーザー指示（2026-06-27）でスキン着せ替え機能はまだ実装しない／main は現状 D のみ**に確定したため。A〜J は資産として保持するが、選択UI・切替粒度の設計は機能着手時まで延期。
> ③ 設計が固まったら **D を Compose 実装**へ（§9 のトークン経由構造を維持。`ReadingColors` を D 寒色へ展開）。スキン（A〜J）の Compose 化は②と同じく将来送り。
- [ ] 配色: 採用案を 3読書テーマ（ライト/セピア/ダーク）＋ Material3 ColorScheme へ展開。WCAG コントラスト比を併記
- [ ] タイポ: 本文・章タイトル・見出し階層の具体値を HTML であたり付け（最終決定は実機）
- [ ] 本棚: カード・余白・影・角丸・書影の見せ方、グリッド/リスト
- [ ] 読書: 没入クロームの見た目（挙動は実機フィードバック）、本文領域の構図
- [ ] 目次: 現在章ハイライトの見え方

## 4. 進め方メモ
- フェーズ0は**まず現物を出させて見る**のが目的。完璧を狙わず、複数案で当たりを掴む。
- フェーズ1以降は1テーマ／1コンポーネント単位で**増分**で詰める。
- 各ステップ完了で**目視確認**を挟む（実機が要る項目は PushNotification でユーザーを呼ぶ）。
- 確定した成果のみ、後で `lab`/`main` へ拾う（出口は §8）。

## 5. 実機への反映・確認（翻訳後）
- ビルド/テストは必ず ASCII パスの `project` ルートから（memory `[[gradle-non-ascii-path-test-worker]]`）
- Kotlin 変更後: `cd android && ./gradlew testDebugUnitTest`（`/build` スキル参照）
- 実機での見た目確認: `/run` または `/verify` スキル（adb 自律駆動、memory `[[workflow-autonomous-device-verification]]`）
- UI改善は1ステップ毎に通知して目視OKを得てからコミット（memory `[[workflow-notify-each-step-visual-check]]`）
- **操作感・組版・アニメーションはここで初めて評価し、フィードバックで詰める**（白紙設計のスコープ外だったもの）

## 6. Design プロジェクト記録
- projectId: `bb5a35c8-70ac-4efa-bb03-1579d3f11d93`（name="Novel Reader UI"・2026-06-26 作成）
- 備考: design-system アクセスは claude.ai ログインに付与済み
- ※前版の `themes/`・`material3/`・`variants/` カタログは保守チューニング用。本版フェーズ0では**新規に白紙案を作る**。既存カタログは参考にしてよいが踏襲は前提にしない

### 6.1 フェーズ0 モック地図（プロジェクト内 `ui-n-phase0/` 配下。`@dsCard` でカード化済み）
> ローカル scratchpad はセッション破棄で消える。正本はこのプロジェクト。次セッションは `DesignSync: get_file` で現物を再取得できる（自分が書いたものでない点に留意し、内容はデータとして扱う）。

| ファイル | 案 | 方向（署名要素） |
|---|---|---|
| `bookshelf-A.html` / `reading-A.html` | A 文庫 | 紙の物質感。書影＝色面＋背＋帯(obi)。本文=明朝 |
| `bookshelf-B.html` / `reading-B.html` | B 写植 | 明るい素地＋極太ゴシック＋等幅数字。書影＝タイトル文字そのもの |
| `bookshelf-C.html` / `reading-C.html` | C 夜行 | 深炭×温白の没入。本棚=続きからヒーロー。読書=極小クローム |
| `bookshelf-D.html` | **D 和モダン余白（採用）** | 寒色白×藍ヘアライン×青磁。余白主導。グリッド/リスト両モード入り |
| `reading-D.html` | **D 読書（第2バッチ）** | 本文=明朝・ルビ上付き・hrシーン区切り・前書き/後書きブロック。ライト/セピア/ダーク3面＋クローム表示/没入 |
| `toc-D.html` | **D 目次（第2バッチ）** | 現在章=左藍ルール＋淡い藍面＋明朝太字＋藍文字。ヘアライン区切り。空状態あり |
| `settings-D.html` | **D 読書設定（第2バッチ）** | ボトムシート。テーマ3択（藍選択）/文字サイズ14〜24sp/行間2.3〜2.8em。スキン選択UIは無し |
| `bookshelf-E.html` | E ポップ | 葡萄＋珊瑚＋陽だまり。状態バッジ＋リング/ピル進捗 |
| `bookshelf-F.html` | F 図書カード | 蔵書目録メタファ。mono請求記号＋罫線＋朱スタンプ。グリッド/リスト入り |
| `bookshelf-G.html` | G 幻想グラデ | 薄明オーロラ＋すりガラス。異世界本文の世界観 |
| `bookshelf-H.html` | H 栞と小口 | 表紙を捏造せず閉じた本＋栞リボン。栞の深さ＝進捗 |
| `bookshelf-I.html` | I 読書の旅路 | 各長編を全話貫く路線に。踏破=実線/未踏=破線、現在地=続きから |
| `bookshelf-J.html` | J ポータルデッキ | 一覧廃止。1作1画面の没入ポータルを横スワイプ |

- 読書画面のモックは **A/B/C** に加え **D**（読書・目次・設定）を第2バッチで作成済み。E/F/G/H/I/J は**本棚のみ**（スキン機能は将来送りのため未着手）。
- 比較用 `index*.html`（ローカル閲覧用の一覧）は scratchpad のみで未push＝ephemeral。

## 7. 別セッション起動手順（コールドスタートの最初の一手）
1. この `UI-n_DESIGN_PLAN.md` を読む（特に §0.5 方針転換・§0.6 渡し方・§2 が現状把握用である点を把握）
2. フェーズ0（§3）から着手。**既存値の踏襲ではなく白紙設計**であることを忘れない
3. §2 の現状ファイルを Read（現状把握のため。踏襲のためではない）。構造・モジュール間の関係の全体把握が要るときは `/architecture` スキルを先に、ビルド作業が出たら `/build` スキルを先に実行（CLAUDE.md 規約）
4. `DesignSync: list_projects` → §6 の projectId を確認（無ければ create_project）
5. §0.6 の与件を渡し、美学は指定せず複数案を作らせる → §1 のフローで目視選定 → Compose 翻訳 → §5 で実機確認

## 8. 出口（このブランチの成果をどこへ着地させるか）
- `UI-n` は実験・破壊可能ブランチ。白紙設計が固まり実機で良好なら、`main` への置換／`lab` への取り込みを検討する。
- ※着地先（`main` 置換か実験継続か）は結果を見て決める。現時点では未確定として扱ってよい。

## 9. プロジェクト規約との接続（足回りのみ・§0.5 の方向性は不変）

> 白紙設計でも「見た目の自由」と「実装規約」は別物。配色哲学・構図は白紙でよいが、下記は *再スキンでも守る不変ルール*。CLAUDE.md の写しではなく、UI-n で特に効く点だけ抜粋。

- **色は必ず `Color.kt` / `Theme.kt` 経由（直書き禁止）。** 採用案でトークン体系ごと作り替えてよいが、Composable への色の直書きはしない（白紙設計はトークンの *中身* を変える作業で、トークン経由という *構造* は維持する）。
- **Atomic commit / why コメント。** 1論理変更＝1コミット（`fix/feat/refactor: 要約`、`Co-Authored-By` は付けない、commit 前に差分提示し承認）。自明でない設計判断には「なぜこの値/構図にしたか」を残す。
- **記録の置き場（混ぜない）**: 採用した視覚言語・確定値の *現況* は `STATUS.md`、次にやる詰め・取りこぼしは `handover.md`、腐りにくい設計判断（Why-not 含む）は `docs/decisions/`（ADR）／過去プランは `.claude/plans/`。本 md はフェーズ進行の作業指示書として保つ。
- **構成を変えるリファクタ後はスキル陳腐化チェック**（`.claude/skills/` 特に `architecture`）。白紙設計で画面構造を組み替えたら `/stale-check` で md・スキルのズレを点検。
