# awaiting-human — 人間待ちの台帳（実機目視・裁定・外部手続き）

> **端末を手に取るとき・何かを決めるときに、まずここを開く。**
> 置くのは **Claude だけでは進められないもの**だけ——①実機で目に見えることの確認 ②意匠・方針の裁定
> ③こちらの手を離れた外部手続き（ブランド名・鍵・Play Console・第三者）。
> **Claude が今すぐ動けるやること＝`handover.md`**（二分した理由と、迷ったときの振り分け規則＝ADR 0028）。
>
> **各項目には「何を見れば決まるか」を必ず書く**——見る画面と OK の条件／選択肢と判断材料の在処／何が確定すれば次へ進めるか。
> それが書けない項目はここに置かない（材料を揃える作業が先＝それは `handover.md` 側のやること）。
> 済んだら打ち消し線で残さず**消す**（完了の正本は git log）。体系の正本＝CLAUDE.md「管理ドキュメントの体系」。

## 1. 実機目視ツアー（OPPO PGEM10・上から順に消化できる並び）

> **前置き**: `gw :app:lintDebug` → `gw assembleDebug` → `adb install -r`（作法＝`/device-verify`・実機 IP は `STATUS.md`・
> 切れたら `adb-bridge`）。**実機には 2026-07-30 04:44 投入の修正版 debug APK が入っている**（`dumpsys package` の
> `lastUpdateTime` で実測）＝以降のコード修正を見るには入れ直しが要る。
> **実蔵書で破壊フローは踏まない**（削除の確認はダイアログを開いて文言を見てキャンセルまで＝memory
> `device-verify-delegation-no-destructive-on-real-library`）。ツアー原型＝`.claude/plans/handover-sweep-2026-07-29.md` 末尾。
> **全画面に共通の観点**＝「初見で迷わないか」（明快K が既定・大原則＝memory `feedback-default-ui-legible-first`）。
> K の6面（本棚／さがす／設定／目次／読書／装いの間）はこのツアーで一通り通る＝K 全体の目視はここで兼ねる。

### 1-0. 2026-07-31 の修正（APK 投入済み・ツアーで一部消化）

> 2026-07-31 の実機ツアーで旧 §1-0 の3件（ダイアログの面色・設定スイッチの未選択トラック・
> 目次の話数ラベル）は**6スキン全 PASS** となり節ごと消した（面色は実画素100%一致・
> スイッチトラックも全スキン `surfaceContainer` 期待値と一致・話数ラベルは折り返し消滅と題名開始 x の整列を確認）。
> ⚠️ **話数ラベルの4桁だけ未検証**——実蔵書は最大 860話で**4桁の本が存在しない**。式に桁上限は無いが実機では踏めていない。
> 以下は**まだ見ていない分**と、見た結果さらに人間の判断が要るものだけ。

- **[作品詳細のあらすじ]** 書影ヒーローを 200→120dp。**開いた最初の画面であらすじが何行覗くか**
  （既読の本で 1.7行 → 約4.7行の見込み）。書影が小さくなりすぎて素っ気なくないかも併せて。
- **[新着タブの更新日時・残るは M だけ]** J は実機 PASS（「07:51 更新」が全可視行に出て pt ラベルは0件）、
  P はコード上 `orderMetricLabel` を3箇所で呼んでおり是正済み（ただし**P のホームは構造上そもそも出ない**＝§3-2）。
  **M だけ未確認**。⚠️ **深夜0時をまたぐ瞬間の表示**が見られたら僥倖（「昨日 更新」へ切り替わるのが正・
  「今日」のままなら暦日判定の穴）。
- **[グリッドの状態行]** 欠落カードで「本文なし・タップで再取込」が1行に収まるか（K と **D/C 両方**）。
  **選択モードに出入りしてカード高が跳ねないか**が唯一の副作用リスク。著者名の無い本でも⋮が右端に留まるか。
- **[星図M の銘]** 幅が 168→196dp 以上に広がったことで、題名の行数や星図キャンバスとの重なりが崩れていないか
  （左右両側の銘・hero も）。
- **[設定「きせかえ」の U+2060 だけ残]** 折り返しは実機 PASS（「（」の直前で割れる・2行）。
  ⚠️ ただし **U+2060（WORD JOINER）が a11y ノード本文に実在する**ことを実測で確認済み
  （生バイト `e2 81 a0` ×9・`（`〜`）`の全字間）。**実 TalkBack で読み上げに漏れないかは音を聞かないと決まらない**＝一度だけ確認を。

### 1-1. 削除警告 — 残るは M/P だけ（指が要る）

**2026-07-30 の実機検証で K / 和モダンD / ポータルJ は PASS 済み**（警告が出る／欠落0冊なら従来と完全に同一／
混在選択の冊数も正しい）。実機の蔵書7冊は**全冊とも本文健在**なので、欠落状態は捨て本を作って再現する必要がある
（＝実蔵書は絶対に消さない。以前これで読書位置・栞・追加日を永久に失った実害あり）。

> **[決着済み・2026-07-31] M/P の長押しは実装不具合ではなかった**——1200ms の長押しで M も P も選択モードに入る
> （M「1 天体を選択」／P「1本選択中」）。旧記述の「M/P だけ長押しがクリック扱い」は **adb 側の保持時間不足**だった。
> ⚠️ ただし **M/P の既定面（星図・カセット）には長押し対象が無く、一覧表示へ切り替えないと入口が無い**点は残る
> （入口の一貫性は §3-2 の裁定枠へ移した）。
- **[削除警告の文言が M/P/J で浮かないか]** M/P/J は欠落バッジが未翻訳＝**棚には何の印も無いのに、ダイアログでだけ
  「本文なし」と言われる**過渡状態。第1波の所見では**予想より悪い**——M は「未読・全1話　まだ星は結ばれていない」＋
  「最初の星を灯す」ボタンで**健全な未読本と表示が完全に同一**、P も「全1話」＋赤い「未読」バッジのみ、J は印なし。
  その状態でダイアログだけが**棚のどこにも存在しない語を鉤括弧付きで引用**する。
  **見るのは「浮くか」**。浮くなら §3-2 の「バッジ・バナーのスキン別表出」を前倒しする。
- **[復旧バナー・ダイアログの文言]** ⚠️ **実復旧はまだできない**——「まとめて再取込」は PDF 本では構造的に常に0冊
  （真因＝`docs/knowledge/auto-backup-does-not-restore-uri-permissions.md`）。実復旧は案X 実装後。**見るのは文言と表示だけ**。
  ついでに**取込元の行が出ないこと**を確認（なろうPDF 由来は `sourceUri` にファイル名が無く、数値IDを出さないため行ごと隠す設計）。

### 1-2. 本棚

- **[Web 複数選択削除]** 可視⋮ → 複数選択 → Web 作品を含む削除で確認文言が出し分いている。
- **[破線間隔 4/3dp・紙地沈め alpha0.05]**（縦画面。2026-07-26 の目視便で言及が無く未裁定のまま）。
  **見るのは**＝破線が細かすぎ/粗すぎないか・紙地の沈みが感じ取れるか。裁定の一次情報＝`.claude/plans/ui-density-swipe-round-2026-07-24.md`。

### 1-3. さがす（K）

- **[気分スワイプ]** 横に振っても下部が動かない（2行を超える組でも）。
- **[ランキング横スワイプ＝試験実装・採否裁定]** 全6期間を横スワイプできる・端で外側タブへ抜けない・期間タブが連動。
  **決めること**＝採用 or 差し戻し。**放置した場合**＝試験実装のまま出荷される。差し戻しは当該コミット
  （件名「さがすランキングを全6期間の横スワイプPager化」）の revert 一発で済むよう隔離済み。
- **[戻る階層 up]** 検索→結果一覧→作品詳細で ←/Back とも1段ずつ上がる・検索条件へ戻るのは「条件を変更」だけ（ADR 0026）。

### 1-4. 画面遷移

- **[要裁定・2026-07-30 実機で発見] 設定タブでシステムバックを押すとアプリが終了する**（本棚タブへ戻らない）。
  マニフェストのコメントは「ルート（本棚タブ page 0）ではアプリ側の Back 割込みが全て enabled=false」と書いており、
  **page 0 以外は割込む建付けに読める**ので、実装意図とズレている可能性がある。
  **何を見れば決まるか**: 設定タブでバック → 「本棚タブへ戻ってほしい」なら不具合（タブ間も階層 up として扱う）／
  「終了でよい」なら仕様（タブは横スワイプで行き来する前提で、Back は階層 up ではない、という設計もありうる）。
  ADR 0026 で発見サブツリーは階層 up に統一済みなので、**タブ間をどう扱うかだけが未定**という状態。
- **[push スケルトン]** 本棚→目次/本文で骨が出て、着地でジャンプしない・話送りに骨が混ざらない。
  余裕があれば framestats で遷移窓が浄化されているかまで（残る計測は handover 側）。
- **[章遷移で描画が上部にジャンプする（再現不能バグ）]** **取ってほしいのは再現条件**＝スキン／縦書き・横書き／
  スワイプ送り・ボタン送り／没入 ON・OFF／既読章へ戻るときか新章か。**これが取れれば Robolectric で赤を出して直せる。**
  調査済みの経路（＝同じ道を再探索しない）は handover「未修正・調査中のバグ」節。

### 1-5. 読書

- **[表示設定ライブプレビュー]** スライダー押下中に一行だけ残ってシート/スクリム/上下バーが退避 → 離すと復帰。
  3本のスライダーが各行に対応・M/P はシート面グラデも退避。候補モック＝`docs/design-candidates/reading-settings-livepreview-D.html`。
- **[縦書き]** 章題が出ない（本文と重ならない）・下端バーが鏡像 [次章←｜目次｜表示設定｜→前章]・横書きに戻すと従来並び。
- **[P2.5 題字]** 縦書きの題字まわりの見え方（実機でしか判断できない残り）。
- **[下端バー]** navigation bar の帯が不透明で左右対称（暗テーマで確認）。
- **[M 装着シート]** 文言が「設定の『きせかえ』から」になっている。
- **[観察・J 中央タップトグルの空振り]** 1回空振りに見えた所見（adb タップ精度の可能性が大）。**人間の体感で再現したら**
  計測タスク化する（決め打ちで直さない＝memory `feedback-perf-jank-is-real-signal`）。
  あわせて「トグル時に軽い状態表示を出す」という改善案もある＝要否をここで判断。
- **[観察・J2 敷居光]** 章末＋クローム表示時に金緑グローが「次章」ボタンへ薄く載る（タップは透過・機能影響なし）。
  心地よいか／chrome 中は抑制すべきか。
- **[観察・M 観測弧]** 進捗の弧がレール/ノードに埋もれていないか。弱ければ太さ/明度を後詰め。
- **[観察・J の glyph 180sp 固定]** 極端に短い章題・広幅端末での見え方。

### 1-6. 取込・通知

- ⚠️ **[Web 取込表示は自動検証が不能＝人の手が必須]**（2026-07-30 に真因判明）: **なろうURLでは永久に踏めない**——
  `SiteAdapterRegistry` の `blockedHosts` に `syosetu.com` があり「公式サイトでお読みください」へ送られて取込が起きない。
  検証には**カクヨム等の対応サイトのURL**が要る。加えて `adb` の `am start` は ColorOS の関連起動ゲート
  （`AppStartConfirmDialogActivity`）に阻まれるため、**人が Chrome から「共有」で渡すのが唯一の経路**。
  以下はその前提で。
- **[Web 取込表示]** 取込中の「停止」が効く（章境界で止まる）／Web バナーにステッパーが出ない（章 i/N のみ）／
  複数重複投入で「N件は取り込み済みです」に集約される。
- **[取込元PDFの同時削除・残り2件]** (c) 削除失敗時の Snackbar は成功パスしか通っていない＝**権限失効の再現機会があれば**見る。
  (d) 端末の永続URI権限128件上限に達したときの縮退（UI 警告は未実装＝要否はこの体感で決める）。検証は捨て本のみ。
- **[U1 新着チェックの Web 統合]** 次に Web 新着が発生したときに自然確認（読了本のみ再フェッチ）。

### 1-7. リリース経路（R8/署名を触ったときだけ）

- **[release で PDF 取込]** SAF 操作が要るため release ビルドで唯一未検証の経路。R8 ルールを触ったら必ず手で通す。

## 3. 裁定（意匠・方針）

### 3-1. 公開に効くもの（決まらないと初回公開の品質が確定しない）

- **[横向きの本棚で書影カードが1枚も収まらない]**（2026-07-30 実機で発見・**構造の裁定が要るので直していない**）。
  実効ビューポートは実機で **~133〜141dp**（ボトムナビ 65dp ＋ ヘッダ ~68dp ＋ 状態チップ行 ~46dp ＋ システムバーが食う）に対し、
  カード実高は5列で **~206〜212dp**（セル幅 124.8dp → 書影 3:4 で 166.4dp ＋ キャプション）＝**構造的に入らない**。
  さらに末尾までスクロールすると画面の 2/3 が空白になる。
  **何を見れば決まるか**＝端末を横にして本棚を開き、どの解を採るか。調査済みの選択肢は3つ（効く順）:
  - **(a) ヘッダ＋状態チップ行（固定 ~114dp）をグリッドの span item へ移してスクロールで抜けさせる**
    → 実効 ~250dp となりカード1枚＋次行の頭が見える。**意匠改変が最小**で、K の「全画面に明示タイトル」とは両立する（スクロールで消えるだけ）。
  - **(b) 横向きのみボトムナビを NavigationRail（左端縦置き）へ** → 65dp＋nav インセットが縦方向から消える。
    Material の canonical な compact-height 対応だが、**全画面に波及する構造裁定**になる。
  - **(c) カード実高を下げる**（列数 5→7 で書影 80dp なら計算上は収まる）→ ただし裁定「書影の肥大回避」の逆方向へ行き過ぎる。
  ※ `Insets.ScrollBottomForFab = 96dp` は真因ではない（`contentPadding` はビューポート高を食わない）が、
  「1枚も入らない画面の末尾に 96dp の空白」は体感を悪くするので向き応答化は副次的に有効。
  ⚠️ **この破綻は golden に写らない**——Robolectric の window insets は全て 0 で、`@Config(qualifiers)` にインセット修飾子が存在しないため。
  合成インセットを流すことは可能だが、その値は端末ごとの推測値＝**推測を回帰の基準線に焼き込む**ことになるので採っていない。
  横向きの検証は実機が唯一の手段。
- **[K の正本モックが実装と食い違う]** `docs/design-candidates/skins/{bookshelf,bookshelf-list,toc,settings,discovery}-K.html` の5枚が
  `--ink-soft:#6A6E78`（素地 4.89:1）を宣言する一方、実装 `OnSurfaceVariantLight` は **#7C808B（3.79:1）**＝
  **モックが「AA を満たす暗化」を決めているのに Kotlin が追従していない**。K は `SkinK : SkinTokens by SkinD` の全委譲なので
  **選択肢は (a) D の値そのものを暗くする〔全スキンに波及〕／(b) K だけ委譲を解く〔K 専用値を持つ〕**。
  **放置した場合**＝ADR 0027 で唯一出荷すると決めた画面の正本が未検査のまま出荷される（検査表への追加も裁定待ちで止まる）。
- **[削除ダイアログの確定ボタン文言]** 実装は「復元せずに削除する」。**モックに前例が無く実装側の自己決定**のため要裁定。
  判断材料＝§1-1 の実機表示。**放置した場合**＝自己決定のまま出荷される（意匠正本に前例が残らない）。
- **[torn 状態の検出定義（`hasContent` の穴）]** 「`index.html` はあるが章ファイルが無い」本は現行の欠落判定に掛からず、
  **バッジもバナーも出ないのに読めない本**が成立する。判別法は確定済み＝**`index.html` の章リンク数と実ファイル数の突合**
  （2026-07-30 実機実測。疑われていた1冊は短編で正常＝実機に torn 本は現存しない）。
  **決めること**＝この照合を `hasContent` に組み込むか（影響がバッジ・バナー・復元経路に及ぶ）。
  **放置した場合**＝現存しない不具合なので公開は止まらないが、将来 torn が生まれると無言で読めない本になる。

### 3-2. 決めなくても公開は止まらないもの（スキン系は ADR 0027 追記で初回公開まで凍結）

- **[章見出しの話数ラベルをどう組むか]**（2026-07-31・**データ側の器は用意済み・描画は未接続で見た目は現状のまま**）:
  正本モック（`reading-D` / `reading-vertical-scroll-D` の `.chap-h`）は `.num`（話数・ゴシック小・アクセント色）と
  `.t`（題・明朝）を**別要素**として規定するが、実装は章題を1本の文字列で抱えていた。分離器
  （`domain/ChapterTitle.kt` の `splitChapterTitle`）と `Typography.GothicFamily` を入れたので、あとは意匠の決定だけ。
  **決めること**は3つ: ①**そもそも話数ラベルを出すか** ②出すなら**原文の接頭辞をそのまま置くか**
  （`０１．` をそのまま）**index から組み直すか**（M/P/J 流の「第 百二十七 話」）③**ゴシック化するか**。
  ⚠️ **横書きの `ChapterHeader` と同時に決める必要がある**（縦書きだけ変えると同じ本が向きで別の見出しになる）。
  **放置した場合**＝現状のまま（章題が1本の文字列で出る）。器は無害に眠るだけで、出荷は止まらない。
- **[選択モードの入口がスキンで揃っていない]**（2026-07-31 実機で判明）: **C/D にはカードの⋮→「選択」という
  タップだけの経路がある**が、**M/P/J の⋮にはそれが無い**（J は「PDFを追加」のみ）。さらに M/P の既定面
  （星図・カセット）には長押し対象が無く、**一覧表示へ切り替えないと選択モードに入れない**。
  長押し自体は全スキンで機能する（実装は無罪）ので、これは**入口の設計が揃っていない**という話。
  **何を見れば決まるか**＝M/P/J の⋮に「選択」を足して揃えるか、「スキンごとに入口が違ってよい」と割り切るか。
  **放置した場合**＝M/P では既定面から削除に到達できず、一覧へ切り替える手順を知らないと詰まる
  （ADR 0027 で初回公開は K 単独なので出荷は止まらない）。
- **[星図M の未選択スイッチが面とほぼ同色]**（2026-07-31 実機実測）: M3 baseline の紫は消えて修正は成功したが、
  M のトラック `#0C1432` は直下のカード面 `#0C1433` と**1階調しか違わず**、輪郭線だけが存在の手掛かりになっている。
  **何を見れば決まるか**＝設定画面でトグルをオフにして「オフのスイッチがそこに在ると分かるか」。
  **放置した場合**＝M でオフのスイッチが見落とされうる（M は初回公開に含まれない）。
- **[カートリッジP のホームでは新着の更新日時が出ない（構造上・意図的）]**（2026-07-31 の追跡で判明）:
  新着タブの指標は `orderMetricLabel` へ集約され D/K/M/J では「05:45 更新」型が出るが、**P のホームだけは
  `HiScoreBoard`→`HiScoreRow`→`boardPointValue` の経路で NEW が `null` ＝スコア列ごと畳まれて何も出ない**。
  理由は `DiscoveryCartridgeP.kt:167-176` に明記＝**HI-SCORE 板は「数値＋固定単位 pt」の造形で時刻を置く器が無く**、
  単位ラベルを変えること自体が意匠変更＝正本モックの前例が要るため。P で更新日時が出るのは結果一覧（`TryRow`）のみ。
  **何を見れば決まるか**＝P の正本モックに「HI-SCORE 板へ時刻を置く」前例を作るか、
  「P のホームは順位と pt だけを見せる面」と割り切るか。
  **放置した場合**＝P のホームでは新着の手掛かりが出ないまま（ADR 0027 で P は初回公開に含まれず出荷は止まらない）。
- **[カートリッジP のダイアログ操作ラベルが AA 未達 3.28:1]**（2026-07-31 検出・全スキン実測の副産物）:
  ダイアログの本文・題字は全スキンで AA を満たすようになったが、**操作ラベル（TextButton）だけは P が未達**——
  M3 の `TextButtonTokens.LabelTextColor=Primary` を引くため P の `#B5564E` on `#DBD6C8` ＝ **3.28:1**。
  **何を見れば決まるか**＝P の正本モックのボタン実色との突合。機械が算出した候補は `--red-lo #8D4139`（4.91:1）だが、
  **色の決定は意匠**なので機械では直していない。**放置した場合**＝`tools/check_design_tokens.py` の a11y ベースラインに
  【要裁定】3件として可視化され続ける（検査は緑のまま・P は ADR 0027 で初回公開に含まれないので出荷は止まらない）。
- **[折り返し是正で浮いた意匠判断2件]**（2026-07-31・実装は暫定解で止めてある）:
  - **星図M の銘ブロック幅**: モックは hero に `width:308px`・非hero に `196px` と**別の幅**を与えているが、
    実装は hero/非hero を同一式で扱っている。**見るのは**＝hero の銘が非hero と同じ幅で窮屈に見えるか。
  - **設定「きせかえ」の値の置き場所**: 実機 360dp では「本棚や画面の装いを変える（現在: 和モダンD）」は
    **折り返し自体が不可避**（モックの 390px 前提と違う）。今回は割れ目を「（」の直前へ移して読めるようにしたが、
    **モックの `.rv` イディオム（値をテーマ行と同じ trailing へ置く）に倣えば折り返し自体が消える**。
    **決めること**＝副文に値を混ぜるのをやめて trailing へ出すか。**放置した場合**＝2行のまま（読めはする）。
- **[未読ラベルのモック追従漏れ3枚]** ADR 0014-D「意味を運ぶ文字は WCAG 4.5:1 ＞ 美学」が実装にだけ入り、
  `discovery/bookshelf-fusion-D`（2.14:1）・`bookshelf-shiori-grid-D`（2.14:1 / 3.79:1）・`bookshelf-shiori-consistency-D`（2.14:1）が
  取り残された（同世代の `bookshelf-mokuroku-D` は追従済み）。**モックの色は意匠＝人間の裁定領域**のため機械では直していない。
  **放置した場合**＝検査の `EXPECTED_SKIPS` に理由付きで残り続ける（出荷画面ではないので公開は止まらない）。
- **[発見ホームのスクロールアンカー崩壊・残2系統]**（機序＝`docs/knowledge/lazylist-loading-full-replace-scroll-reset.md`）
  - **① D/C・M・J・P の初回ロード表示を骨へ変えるか**: process death 復帰時に同じ機序で落ちうる。塞ぐには
    `DiscoveryHomeContentTest` に明記された既存裁定「初回は骨格を出せない＝進捗インジケータで待つ」の**変更**と
    4スキンの可視変更を伴う。**やるなら一括**。**放置した場合**＝復帰時の稀な位置飛びが残る。
  - **② 結果一覧(Result)系4実装**: 「新クエリでは先頭表示が正」という解釈があり得るため未裁定。
    ⚠️ `.claude/plans/handover-sweep-2026-07-29.md` の進行記録には「⑨ Result 先頭リセットが正＝現状維持」と裁定した旨があるが、
    コード側（`DiscoveryHomeRegistry.acknowledgedOutOfScope`）の登録理由は「未裁定」のまま＝**どちらが正か確認が要る**。
    裁定して「位置を保つのが正」なら登録を外して不変条件の対象へ移す。
- **[スキン意匠の裁定群（M/P/J）]** 一次情報＝`.claude/plans/ui-refine-richness-round-2026-07-18.md`。
  **放置した場合**＝初回公開には一切影響しない（ADR 0027 追記で保守スコープごと凍結・解禁は課金アップデート便と同時）。
  - **J 扉/升の色相偏り**: fmix32 後も 5/6 扉が緑系。真因の一半はパレット自体が緑系2/4。
    選択肢＝(a) 森テーマとして許容 (b) モックの4世界の色相差を広げる (c) 割当を「直近と異色相優先」の巡回に。
  - **検分ラウンドの ①〜⑦**（いずれもモック正本由来＝直すなら意匠変更）: ①J だけ代替モードが「1冊全画面デッキ」
    ②D 本棚の「N話」だけ総話数 ③M 読書クローム章題の淡さ ④M の装いの間入口が✦星 ⑤M 設定はテーマ固定1枚
    ⑥読書クロームのアクセント色が P だけ実装で付く ⑦ADR 0022 §3「P/J は静止」と reading-J の敷居光 breathe の矛盾
    （ADR 追記で承認例外にするか実装を抑えるか）。
  - **M 読書 下部バーの微星装飾**: 忠実実装には M 専用バー面の新造が要る（発明回避で見送り中）。要るなら意匠込みで裁定を。
  - **M「迎える」/ J ⋮ の FAB 置換モック**・**精査待ち ③HI-SCORE 1位金の微燐光パルス**（P 静止則の例外承認が要る）。
  - **バッジ・バナーのスキン別表出（M/P/J 翻訳）**: 蔵書復旧バッジ・取込バナーの Web 0%凍結（`SkyProcessingBanner`/`WritingBanner`）。
    モック未裁定のためスキン後回し枠。§1-1 で文言が浮くようなら前倒し。
- **[ハーメルンの規約まとめ裁定]** `pendingHosts` で保留続行中（正本＝ADR 0024 追記）。
  **放置した場合**＝対応面の拡大は「打ち止め」と裁定済みなので何も困らない。解放するなら blockedHosts/pendingHosts 行の削除＋アダプタ追加だけで着手可。

## 4. 外部手続き・人からしか出てこないもの

- **[ブランド名の確定]** ★**最大のブロッカー**。`applicationId`（公開後は永久変更不可）・ストア掲載素材・
  プライバシーポリシーの公開先リポジトリが**全部これ待ち**。**何が確定すれば進むか**＝商標チェック
  （J-PlatPat 称呼検索＋ストア同名確認＋ドメイン/SNS 可用性）を通った固有名1つ。**放置した場合**＝公開が1歩も進まない。
- **[鍵と資格情報のバックアップ]**（ユーザー作業）: `/mnt/c/Users/qingj/keystores/novel-reader-upload.jks`（RSA 2048・2053-12-14 まで・
  alias `upload`）と `android/local.properties` の `release.*` 4行。**放置した場合**＝紛失時に Play への鍵差し替え申請が発生。
  Play Console 照合用の upload 証明書 SHA-1 = `FB:ED:C1:33:BC:1B:44:CA:35:B5:99:0D:47:6B:7C:27:B4:03:43:E3`。
- **[ストア掲載素材の作成]** アイコン 512²・フィーチャーグラフィック 1024×500・スクショ最低2枚（1枚目＝PDF ふりがな付き縦書き／
  本棚を文言付きで・**明快K で撮る**）・タイトル30字・短い説明80字・詳細4000字。命名確定が前置き。
- **[Data safety の提出]** 下書き＝`docs/store/data-safety-draft.md`。残るのは**提出時に実フォームの設問文言と突合**するだけ
  （Auto Backup は「収集なし」で申告済みの裁定・審査指摘時のみ保険申告へ切替）。
- **[プライバシーポリシーのプレースホルダ確定と公開]** 下書き＝`docs/store/privacy-policy-draft.md`。
  **確定が要るのは**アプリ名／屋号（組織アカウントの公開名義と一致必須）／問い合わせメール／公開日。公開先＝GitHub Pages（裁定済み・公開専用リポジトリ）。
- **[In-App Review の実表示確認]** **内部テストトラックへ配信しないと確認できない**（sideload では ReviewManager が no-op）。
  実装とトリガ（初回読了の false/null→true 遷移・セッション1回）は完了済み。
- **[第三者ユーザビリティテスト]** 本人テストが成立しない2件＝**T3**（二読書面の操作言語混乱）・**T4**（中央タップトグルの再発見。
  ヒントは通算1回きりで再表示条件なし）＋**実 TalkBack の音声走査での是正確認**（uiautomator の静的確認では customActions が見えない）。
  プロトコル＝`.claude/plans/usability-test-protocol-2026-07-12.md`。
- **[C 夜行スキンの「何が違ったか」の言語化]** 欠落モック初稿が「全然違う」で差し戻しになったまま（ドラフト＝`skins/candidates/c-draft/`・
  `skins/candidates/list-C/`）。**憲法2枚（bookshelf-C / reading-C）の読み込みだけでは足りないと実証済み**＝再着手はイメージの聴取から。
  あわせて⚠️ **人間の実機目視で「これが夜行だ」と確認できた事実はまだ一度もない**（色トークンだけでは D ダークと区別がつかない）。
  **放置した場合**＝C スキンの再着手ができない（初回公開には無関係）。
- **[特商法表記]**（フェーズ2・課金時）: 買い切り IAP 販売開始時に必要。住所/電話の省略運用可否は専門家確認。
  掲載＝アプリ内 or プライバシーポリシー同居 URL。資金決済法は買い切りのみなら非該当。
